# Backlog — index of every plan doc and open item

**This is an index, not a backlog of its own.** Each row points at the doc that owns the detail;
the "What's left" column exists only so you can tell, without opening twenty files, whether
something is still live. When work lands, update the owning doc first and this table second.

Finished plans live in **`archived/`** (see `archived/README.md`) — kept for the reasoning, not
tracked. A plan moves there only once it owns no remaining work.

**When something is done, it leaves "Open features" the same day.** Three steps, and they are one
job, not three:

1. Lift any surviving follow-up into `Cleanup_Tasks.md` (or into whichever live doc will own it),
   so nothing is dropped by the move.
2. Move the doc to `archived/` if it now owns no remaining work, rewriting its links both ways —
   inside it a doc still in `docs/` becomes `../Name.md`; pointers to it become `archived/Name.md`
   from here, a bare name from inside `archived/`, and `docs/archived/Name.md` from source files.
3. Cut the row down to **one line** in **Done** below: what shipped, and what the doc is worth
   keeping for. The long version belongs in the owning doc, which is where a reader who wants it
   is going anyway.

Step 3 is the one that gets skipped, and skipping it is how eleven finished things ended up filed
under "Open features" (found 2026-08-23). A done row that stays put is not a small untidiness: it
defeats the one question this table exists to answer at a glance.

**Small cleanups live in `Cleanup_Tasks.md`, which owns them outright — this file points at it and
does not list them** (2026-09-08). It used to carry three tables of them, billed as a roll-call, and
they went stale in both directions: three of nine deferred items were listed, and two done items had
grown a *second, independently written* account here alongside the one in `Cleanup_Tasks.md`.

**The rule that replaces "keep them in sync": a row here may not carry a fact that is not in the doc
it points at.** Then going stale is impossible by construction rather than by discipline — the same
argument CLAUDE.md makes for a test that must not need editing on every change. If you find yourself
adding detail to a row, the detail belongs in the owning doc and the row belongs shorter.

**The loss this accepts, named out loud:** this file no longer answers "what small work is
outstanding" — it answers "which plans are live". For the first question, open `Cleanup_Tasks.md`,
which is where you are going anyway to do the work.

Decisions made during implementation that still need Ted's eye live in `DecisionsToReview.md` —
a review queue, not a backlog; work through it one entry at a time.

Status verified against the tree on **2026-09-04** (`ba1b0ad`). The year overview's row was found
sitting in **Open features** marked `done`, and moving it is what the three steps above are for.
**One more is still there:** `PlannedPrivateEventsListPlan.md` is marked `shipped` with "nothing is
open", so it wants the same three steps — left in place rather than moved in the same pass, because
its sibling `ChangePrivateEventPlan.md` is `partial` and may yet claim work from it.

Legend: `open` · `partial` · `done` · `exploration` (deliberately unbuilt design record)
· `decision` (waiting on Ted, not on code)

---

## Open features

| Item | Owning doc | What's left |
|---|---|---|
| **Cancel Train, the overlapping-legs detector, and entry-time enforcement** | `CancelTrainAndOverlappingLegsPlan.md` | `partial` — reported and planned 2026-09-06 (Ted); **slice 1 (Cancel Train) SHIPPED the same day**, slice 2 (the detector) is next. Shipped: additive `TrainCancelled(TrainTripId, String reason)` at schema_version 1 (no bump, no upcaster, no migration, backup stays v3) + `CancelTrainCommand`/`Context` reusing `TrainNotFound`; `CancelTrain` folding existence from the event stream (deliberately unlike its sibling `ChangeTrain`, which reads the details projector); removal branches in **seven** consumers and, deliberately, **not** in `LocationAuditProjector`; `/booked-trains/{id}/cancel` (GET confirm + POST) with `cancel-train.html`, its `SecurityConfig` matcher and matrix row; `Cancel` after `Edit` on `/booked-trains`, a bin after the pencil on the itinerary, and **pencil + bin on `/calendar`** — `EntryDetails.Train` is the first kind with two owner actions, so `CalendarViewBuilder` grew a `pencilAndBin` arm whose paths are independently optional so the pencil never moves. Suite green both tiers (2275 + 78); **mutation-verified in two rounds** — neutralising all seven removal branches failed exactly seven propagation cases and left the eighth (the `LocationAuditProjector` case, which asserts the opposite) green, and removing the matcher failed the matrix row alone. Two things worth keeping: a `doesNotContain("cancel-bin")` assertion failed for the wrong reason because `.cancel-bin` is in the always-rendered CSS block (the whole-attribute form is the real claim, exactly as CLAUDE.md's precise-HTML rule predicts), and the row's action cell became a `div` inside a `subgrid` row — verified headlessly at 1024px and 560px, columns still aligned, stacked layout still stacks, no horizontal scroll. **Slice 2 (the detector) SHIPPED 2026-09-06** — `ScheduleProblem.OverlappingTravel` on the half-open `overlapsWith` predicate (a connection is not a conflict), `ScheduleTimeline.overlappingTravel()`, `TravelLeg` (sealed, typed ids, `label()` + `detailsPath()`) carried on `Movement`, `allLegs()` given a total order, and arms in all four map-over-a-problem sites. **`ProblemRef` renamed `ProblemKey`** (7 files, 22 occurrences, `?problem=` unchanged). **`ScheduleProblemsRenderer`'s five `instanceof` filters became one exhaustive `switch`** in a private `Sections` record, closing the Cleanup_Tasks hole that would have let this very variant render nowhere. Suite green both tiers (2305 + 78); mutation-verified in three rounds. **Three findings.** (1) **The plan's stated reason for the tiebreaker was wrong** and its first test pinned nothing: mutating the tiebreaker away failed it 0/5, because a `ConcurrentHashMap` over a fixed key set iterates deterministically, so "stable across recomputes" can never fail. The test now asserts the *ordering rule* over 25 id pairs and fails 3/3; the tiebreaker stays on the honest reason (the order is ours, not an artefact of UUID hashing). (2) A too-loose `doesNotContain` on a CSS class name again — twice in two slices, the standing hazard of these renderer tests. (3) j2html emits `href` before `class`. **Verified visually at 1024px and 820px**: production's 51-char service id still fits its chip, no horizontal scroll. Three mutually overlapping legs give three rows, one per pair, deliberately. **The payoff, as built:** a reported pair offers **one "Cancel …" link per side**, pointing straight at the `/booked-trains/{id}/cancel` page slice 1 just built — `cancelEachStay`'s shape exactly (the report never decides which leg is the real one, and never POSTs). **Its wording is an open decision**, because the hotel pattern does not survive the move: a train's natural name is its service id, and production says that fails both ways — all 12 stored trips carry one but the form accepts blank, and two *duplicate* legs with no service id fall back to the route, which is **identical on both sides**; meanwhile the longest stored service id is **51 characters** (`S9 (35945 to Hanau Hbf) & ICE 1653 (to Dresden Hbf)`), with four more over 30, which no chip can carry. Recommendation: departure time first, then as much name as fits (`Cancel 9:00 AM · ICE 597`), with the full detail on the card. A true exact duplicate has no discriminator at all and both links read the same — honest, since the entries are interchangeable. **Slice 2's other parts:** **Two findings, both verified by probe.** (1) **Two overlapping legs raise nothing at all**, and there is no leg-vs-leg detector in the tree: `computeProblems` compares stays with stays and occupancies with occupancies, never two `Movement`s. The mechanism is the point sort — a leg contributes a rank-1 `REQUIRE` at departure and a rank-1 `ARRIVE` at arrival, so when the times **overlap** both `REQUIRE`s land before both `ARRIVE`s, every `REQUIRE` agrees with the city the walk is holding, and the contradiction vanishes. When they do **not** overlap the first `ARRIVE` has already moved the walk, so the second departure raises a gap — visible, but wearing the sentence *"No travel — Berlin → Hamburg"* for a journey Ted never meant to take. Worse than the missing row: the bogus leg **silently relocates the walk** (last `ARRIVE` by UTC wins), and that city then feeds `locationByNight`, `awayDays()` and `atHomeOn()`. `duplicateHotels` exists on *"Ted can only sleep in one of them"*; **"Ted can only be on one train"** is the same argument with no detector. (2) **There is no Cancel Train** — `/booked-trains` rows offer Edit only; cancel exists for hotel, ground transfer and private event alone. **The two compound, which fixes the slice order**: a duplicate-leg problem's fix link *is* a cancel page, exactly as `DuplicateHotel`'s is, so the detector without Cancel Train would be a permanently red row. **Slice 1** is `CancelPrivateEvent` with the types swapped: additive `TrainCancelled(TrainTripId, String reason)` at schema_version 1 — **no bump, no upcaster, no migration**, backup stays v3 — plus a removal branch in **seven** consumers and, deliberately, **not** in `LocationAuditProjector` (its own javadoc settles it for hotels: the `TrainBooked` row stays in the log forever and the read-time upcaster resolves its zone on every replay). `LocatedEventsReachScheduleProblemsTest` **cannot** catch a missed branch — `TrainCancelled` carries no `TrainStationAddress` — so a new `TrainCancellationPropagationTest` is the only guard. New matcher **`/booked-trains/*/cancel`** (the existing `/booked-trains/*` is one segment and does not cover it) + matrix row. **Slice 2** adds `ScheduleProblem.OverlappingTravel` on the `Occupancy.overlapsWith` predicate (half-open, so a connection is not a conflict), which needs `ScheduleTimeline.Movement` to gain a sealed `LegRef` identity and `allLegs()` to become a **total** order or every `ProblemRef` key flips between recomputations. Four of the five sites it touches are compiler-forced; **`ScheduleProblemsRenderer` is not** — it partitions by five `instanceof` filters, so a sixth variant renders nowhere and nothing fails, and the slice converts it to an exhaustive switch. **All five questions settled 2026-09-06 (Ted), one by measurement.** Hard removal, not a tombstone (the hotel's greyed row exists because Cancel Hotel has no undo; a train has no deadline and no money story, and slice 2 links here to *remove* a duplicate). One verb, "Cancel". Pencil **and** bin on the calendar train entry, the end state `ChangePrivateEventPlan.md` already named. **`ProblemRef` is renamed `ProblemKey` and the new type is `TravelLeg` — different names because they are different things**: one is a content-derived string that survives in a URL, the other a typed in-memory identity, and `Ref` was vague precisely because it was stretched over both (7 files, 22 occurrences, 2 file renames, `?problem=` unchanged, so no URL impact). The leg identity is a **component on `Movement`** with the problem carrying its own `OverlappingLeg` record, following `DuplicateHotel`/`DuplicateStay`; promoting `Movement` to a public sealed type was rejected as putting the timeline's working type inside a read model. **The overlap rule was validated against `jittertravel-backup-production-2026-09-06T140455Z.json`** per the `EnteredLocation` precedent — 32 live legs (16 flights, 12 trains, 4 transfers), **0 overlapping pairs, 0 zero-length legs, 0 exactly-touching pairs, tightest real connection 33 min** (Eurostar into London 10:57Z, GWR out 11:30Z). So **no false positives**, and **Cancel Flight does not ride along** — there is no overlapping flight pair to fix. The pair to watch is the one transfer that sits near another leg: the Aschaffenberg taxi, **36 min** after the ICE from Aachen; if it ever misfires, exclude a pair kind rather than adding a tolerance. **Provenance confirmed by Ted 2026-09-06: the two-trains case was local, entered while testing** — not production data, and it is not in the log. That cuts both ways and the plan says so. It **strengthens slice 1**: Ted entered data he did not want and had no way to remove it, which is the "a wrong entry with no way out" argument that put Cancel before Edit for private events, met first-hand — and Cancel Train is also what makes local testing cleanable. It **strengthens slice 2 too, and the plan's first reading of that was wrong**: the draft argued "never occurred in real data" lowered the priority, which measured the wrong thing — **zero production incidence was the outcome of Ted's own process, not evidence of low risk**. Ted, 2026-09-06: *"i almost entered the wrong data in production and would have if i didn't test locally first, so the missing problem detection is a real concern."* The local instance was the **control that caught it**, and `/schedule-problems` exists to be the control when there is no other one; a clean log is also what it looks like when the net is absent and nothing has fallen through yet. **The near miss is the evidence**, and it is exactly the evidence a report of already-happened problems cannot produce about itself. **Both slices build, in the original order.** **One finding does not wait**, being a hole today and independent of the feature: `ScheduleProblemsRenderer` partitions by five `instanceof` filters while the three other maps over the sealed `ScheduleProblem` are exhaustive switches, so a sixth variant would be keyed, linked and drawn on the calendar yet absent from `/schedule-problems` itself — logged in `Cleanup_Tasks.md`. **Slice 3 (enforcement at data entry) SHIPPED 2026-09-06** — four new domain types (`ScheduledLegId` sealed over flight/train with `path()`, `ScheduledLeg` with the half-open `overlaps`, `ScheduledLegs` with `overlapping(self, …)`, `OverlappingLegRefused`), `LiveScheduledLegs` folding the stream once for all four write paths (keyed by leg so a `*Changed` replaces rather than doubles, `TrainCancelled` applied), and `OverlappingLegNotice` at the boundary. **The link could not go in the error message** — Thymeleaf escapes `th:errors` — so the text lands under the departure-time input and the anchor rides beside it as a model attribute, with one `.overlap-link` rule in `site.css` (the flight templates have no `<style>` block). Suite green both tiers (2340 + 78); mutation-verified three ways — removing the self-exclusion failed 4, closing the half-open comparison failed 2, dropping the cancellation arm failed 1. **Three findings:** it exposed a **real test-isolation gap** (`truncateAllTables` clears the DB but not the in-memory event list `eventsForDecision` folds from — harmless until now because every earlier fold asked about a *specific id*, this being the codebase's first cross-aggregate question; not a production bug, since a truncate already requires a restart); `NoFullyQualifiedClassReferencesTest` caught an FQCN in a test helper; and **this plan's warning about flight forms was overstated** — they already render `th:errors` per field and already call `rejectValue`, lacking only the four *reporting* rules, so the refusal lands correctly on all four forms today. Originally added 2026-09-06 on Ted's question on Ted's question *"when can we add enforcement of overlap during data entry?"* — designed, mechanism open. **Scope settled: flights and trains** (four commands, checked *across* kinds since a flight overlapping a train is the same impossibility); **ground transfers excluded on the measurement, not on taste** — their times are approximate by design and the Aschaffenberg taxi sits 36 min from its ICE, which is where a false block would land. **Also settled: wherever the existing leg is named it is a link** to that entry's details page, or its edit page when none exists — today the edit page for both kinds, since neither has a read-only detail page; not the pencil, so the one-icon-one-meaning rule is untouched, and every surface is OWNER-only. Same rule applies to slice 2's card. **Sequenced after slice 2 so the overlap predicate exists once** — two copies are two chances to disagree about what an overlap is. **Two traps recorded:** `ChangeTrainCommand` must exclude the trip being changed or no booked trip can ever be corrected, and the **flight forms have no field-level errors yet** (CLAUDE.md's 2026-09-06 form rules cover the two train forms only), so covering flights means extending that first or shipping two error experiences for one rule. `BookTrainContext` is one `Instant` today and would become the **first decision context carrying a collection**, folded from the event stream per R1. **Mechanism settled 2026-09-06 (Ted): refuse outright** — rejected at the command like `EnteredLocation` rejects a station-as-city, field-level message, nothing written, no new form pattern. The cancel-first objection dissolves against Ted's other two answers: slice 1 made cancelling one link from three surfaces, and the refusal **names the blocking leg as a link**, so the error is not a dead end. Warn-and-proceed and save-then-report were both rejected for the same reason — this is the near miss that nearly reached production, and both leave the bad entry writable. **`BookTrainContext` growing from one `Instant` to a collection is deliberately NOT filed as a cost** (Ted: *"it's a pattern that will become more common as UI tasks become more complex"*) — a decision that depends on what else is on the schedule can only be made honestly by a context carrying it, the alternative being a command reaching for a read model, which R1 forbids. So the fold is built to be copied: one shared value for both flight and train paths, folding *live* legs (applying `TrainCancelled`), kept in `domain`. Also fixes a stale line: `Cleanup_Tasks.md:790` still says private events have no cancel. |
| **Per-role read models — should FAMILY have its own projectors?** | `PerRoleReadModelsExploration.md` | `exploration` — asked by Ted 2026-09-04 while settling how the owner reaches a conference detail page FAMILY may not open; **deadline to explore: Tuesday 2026-09-08**, set by Ted. Nothing designed. **The finding that frames it:** FAMILY is served the OWNER's read model and the renderer declines to draw the owner-only parts, so **five `EntryDetails` records already carry a path family receives and never sees** (`Gathering`/`Flight`/`Train`/`Lodging.editPath`, `GroundTransfer.cancelPath`) — a conference `detailPath` would be the sixth, so this is the established shape rather than anything the conference work introduces. **For:** `PublicCalendarProjector` is already exactly this, and CLAUDE.md is emphatic that its allow-list beat the redactor's deny-list — family is currently served by that deny-list pattern one role over. **Against, for now:** the argument that justified the public split was specifically about *anonymous* as a threat model, and **R12** forbids a read model built from another, so a FAMILY projector must fold the events again — five calendar projectors plus `CalendarAggregator` plus the itinerary's. **The trigger named:** every owner/family difference today is *an action being absent*, which omission expresses fine; the day one becomes *a field with a different value* (a different label, a coarser time, a redacted city), the split earns itself. A cheaper middle step is logged too. |
| **Conference detail page + Change Conference** | `ConferenceDetailAndChangePlan.md` | `partial` — **only slice 3 (Change Conference) is left, and it is deferred.** **Slice 2 shipped 2026-09-05:** `/conferences/{id}` is the app's **first read-only detail page** — `ConferenceDetailController` + `ConferenceDetailRenderer` (j2html) + `ConferenceDetailView`, built by `ConferenceProjector.detailById` as the dashboard row plus the one field it may not carry, `AttendanceBasis`. Five panels (When / Where / Attendance / Call for papers / Talk) and an actions band; unknown or malformed id redirects to the list. **Slice 4 folded in with no pencil** — the *title* carries it, since a pencil means edit and there is nothing to edit. The OWNER's conference name now links there from all three surfaces; everyone else keeps `infoUrl`, which the detail page carries. **Three things the plan did not predict.** (1) `ConferenceActions` was **extracted** — the detail page is the state machine's second caller, and a second copy is two chances to disagree about which transitions are legal; it carries its own `CSS` constant the way `DisclosureMenu.CSS` does. (2) **Mutation-verifying found a real bug**: reading the acceptance before the recorded basis made a ticket bought *before* a talk was accepted say "A submitted talk was accepted, which is what committed you" — false, he was already going. So the basis/stream precedence here is the **opposite** of `ConferenceProgress.speaking()`'s, deliberately: there the stream must outrank a stale annotation, here the question is why *Ted* is going and a confirmation is the reason he gave. (3) The nav gets this page's own path, not `/conferences` — marking the list active renders it as a non-link span, and the nav is the way back. **Slice 1 shipped 2026-09-04** (`b380f0b`: the venue under the city on `/conferences`). **Slice 3 DEFERRED by Ted 2026-09-04**, on the sharp observation that the change he actually wanted — recording a CFP that has since been announced — *is a CFP flow, not a conference change*, and that flow already exists at `/conferences/{id}/cfp`. It was merely **invisible**: the link was muted text with a hover-only underline, fixed in `628c22d`. That deferral **dissolved Q1, Q2, D4 and D5** — every one of them was a decision only because a change form existed. What is deferred with it: conferences planned before 2026-08-22 carry `infoUrl = ""` and can never be given one, which is the one concrete trigger to bring slice 3 back. **Entry points settled 2026-09-04, and Q5 dissolved with them:** for the OWNER the conference **name** links to the detail page on all three surfaces (`/conferences`, `/calendar`, `/itinerary`); everyone else keeps `infoUrl`, which the detail page then carries. No new icon, and **slice 4 folds into slice 2** because a pencil means edit and there is nothing to edit. Two knock-ons: `CalendarViewBuilder:369`'s "a title link always means go look at this elsewhere" is **dropped as unjustified** — where a title goes is a judgment call, deliberately not a rule — and **hotels are out of scope** (the same reasoning would move a hotel's title off `mapsUrl`, but Ted wants to understand how he uses the app arriving at a hotel first). Plumbing the plan missed: `EntryDetails.Conference` needs a `detailPath` (and `PublicConference` must not get one), and `ConferenceItineraryEntry` carries no `conferenceId` at all. **D1 reframed:** Ted was "never happy about reusing edit pages as a substitute for a real details page", so this is the first instance of where the pattern is going, not a divergence from it. Ted asked 2026-09-04 where a planned conference's details are; **planned the same day, nothing built**. Three findings: there is no `/conferences/{conferenceId}` (flights, hotels, trains and gatherings all answer at `/{collection}/{id}`); `ConferenceView` **carries `venueName`/`venueAddress` and `ConferencesRenderer` renders neither**, so the venue is visible only on `/itinerary`; and a conference is the only planned kind that cannot be corrected. The *edit* half was tracked in `Cleanup_Tasks.md`, the *view* half by nothing. **D1 departs from the house pattern:** everywhere else `/{collection}/{id}` **is** the change form, but here it is a read-only j2html detail page with the Thymeleaf form at `/{id}/change` — because the CFP state that motivated the page is not editable, and the j2html/Thymeleaf split plus the standing "split combined edit/cancel pages" cleanup both point the same way. Collapses to one Thymeleaf page if Ted prefers route consistency (**Q1**). **Three constraints found in the code, each closing an option:** `/conferences/*` is *already* OWNER-only so no new matcher is needed (but the `AuthorizationMatrixTest` row is what proves it); the Actions column is budgeted for exactly **three** links and the state machine already spends all three, so a "Details" action would force a dropdown against CLAUDE.md's rule — which is why the recommended entry point is the **conference name**, with `infoUrl` demoted to an icon; and `EntryDetails.Conference` may gain an `editPath` only because `PublicConference` is a separate record, needing both redaction tiers. **Entry points, one option set per surface:** `/conferences` (name link ★), `/calendar` (pencil in `ownerActions` ★, filling the reserved slot), `/itinerary` (pencil beside the title ★, one line, and the itinerary is already the only place the venue shows), `/schedule-problems` (leave alone ★ — its links mean *fix*), the iCal CFP alarm (`URL:` deferred — the page is behind a login) and the year overview (no, by the zoom-out rule). **D3** is the part to get right: `null` CFP means four different things and the panel must say four different sentences. **Two traps named:** `ConferenceChanged` is a full snapshot, so a form omitting `format` silently resets every edited conference to `CALL_FOR_PAPERS`; and `CfpOpened` derives its zone from the conference's dates, so changing the venue zone leaves the stored deadline meaning a different instant (**Q2**). **D5 extends `EnteredLocation` validation to conferences** — the first time a conference's city becomes typeable — which CLAUDE.md flags as "a decision, not a chore". Four slices; slice 1 (the venue on `/conferences`) needs no route, event or projector change and is the whole of the original complaint. |
| **Booking provenance — how a booking was made** | `BookingProvenancePlan.md` | `open` — requirement captured 2026-08-19 (Ted), **designed 2026-08-24 (Ted), nothing built**. Every booked hotel, flight and train records the **provider it was booked through** — Booking.com, Expedia, railfinder, the hotel, the airline, DB — because that decides **where the ticket is** and **how (and by whom) it can be cancelled or changed**. **Design settled 2026-08-24, seven decisions:** **one free-text String, the provider name** (not an enum, not channel+name — full provenance is rarely a short phrase, but the provider is the load-bearing part *because it is the index into Ted's email*, and it renders without driving anything, so the 2026-08-23 enum-vs-text rule puts it on the text side); **slice 1 is the provider alone**, with reservation number and manage-URL deferred until real use says typing them beats searching email; **additive components on the six existing `*Booked`/`*Changed` events**, `null → ""` in the compact constructor, **no schema bump, no upcaster, no migration** (the `GroundTransferPlanned.mode` precedent — `ConferenceFormat` is the contrast), which closes the separate-event alternative since its only argument was the bump; **OWNER only**, `/itinerary` out (it is `hasAnyRole("FAMILY","OWNER")`, which the original capture contradicted); **surfaces are the three `/booked-*` list columns and the three detail/edit forms** — the cancel page is out on a **rule, not a preference**: reaching it means Ted has *already* cancelled with the provider and is recording that, so **a recording surface needs no decision-support information** (he needed the provider one step earlier, on the list page), the same reasoning that removed the check-in gate; the iCal feed was simply not wanted; **backfill by a declared `OneOffTaskRegistry` entry** recording end state through the real change forms, as the conference pass did in prod 2026-08-21; and **sequence after `ScheduledTransitTripPlan.md`**, which touches the same two train events and forms. **Watch the full-snapshot trap:** the three change forms must round-trip the value or an edit silently clears it, exactly as `HotelChanged`'s javadoc records for `cancelBy`. **Redaction unchanged and now strongest-form:** never on an `EntryDetails.Publishable` record, `PublicCalendarProjector` never reads it, and per the surface decision it reaches no calendar entry at all — not carried and stripped, but never carried. |
| **Post-deploy task banner** | `PostDeployTaskBannerPlan.md` | `partial` — **slice 1 shipped 2026-08-19** (registry + `OneOffTaskCompleted` + OWNER-only home banner + `/admin/tasks`, two real tasks declared; suite green 1046 unit + 36 js). **Both declared tasks were run in production 2026-08-21 and retired the same day** — the conference-attendance backfill (`13:03:29Z`) and the `event_log.type` normalization (`15:27:49Z`) — so `OneOffTaskRegistry` is now **empty**, which is a normal resting state and not a gap: the banner hides itself and `/admin/tasks` reads "Nothing declared." Each `OneOffTaskCompleted` event stays as history and nothing refers to the ids. That exercised the whole latch end to end, declaration through retirement. Steps 2–3 open: derivable checks (legacy-row count, missing config) and the `declaredOn` age check. Designed 2026-08-19. A one-way latch for **one-off tasks that follow a deploy**: migrations to run and backfills of data that did not exist before (scope narrowed by Ted — a hotel with no `cancelBy` is deliberately NOT a task, and a `CALL_FOR_PAPERS` conference with no `CfpOpened` is at most a low-priority timeout). Tasks are **declared in code** and only the completion is an event (`OneOffTaskCompleted`, appended via `CommandExecutor.appendEvents`) — Ted's original sketch latched the declaration in with a **startup append**, which the doc recommends against: it would throw in read-only mode (exactly when the banner is most needed), needs per-boot idempotency, writes deploy artifacts into the backup stream, and would outlive the code that explains it. Splits **derivable** tasks (count legacy rows, missing config — self-clearing) from **acknowledged** ones (a backfill cannot be detected: `WATCHING` with no confirmation is indistinguishable from genuinely undecided). Renders as an **OWNER-only** banner at the top of the home page, same slot/shape as the read-only banner but gated like `showDataEntryNav`. The "don't leave inert code lying around" loop is **advisory, never blocking** — a pre-push hook would need a token-gated endpoint and fails offline, so the doc recommends starting with a `declaredOn` age check in the test suite, with the weekly iCal heartbeat as the other no-network option. **First real customer already exists:** the `event_log.type` normalization from `5df5358` is outstanding against prod. **All five open questions answered by Ted 2026-08-19:** ticking off lives on a separate `/admin/tasks` page (banner links to it, so the route needs a matcher + matrix row); a completed-but-still-declared task shows **only there**, greyed "code can be removed", never in the banner, which disappears entirely once nothing is outstanding; **missing config is a task in the same list** (derivable, self-clearing, covers the only silent failure mode); **restart-after-restore is out of scope** and belongs with the read-only banner instead, since it means the data on screen is stale *now*; and the cleanup nudge is the `declaredOn` age check alone — the pre-push hook and its token-gated endpoint stay unbuilt. |
| **Replace Hotel** | `HotelCancelReplacePlan.md` | `partial` — Phases 0, 1 (`cancelBy`) and 2 (Cancel Hotel + deadline column) are done. Phase 3 (Replace: cancel the old booking and book a new one linked by `replacesHotelBookingId`) is unbuilt and needs a second `HotelBooked` schema bump. **Owning doc is kept as history and now carries a superseded banner (2026-08-13):** the check-in gate it specifies throughout was removed (`CancelHotelContext` is `bookingExists` alone; `CannotCancelAfterCheckIn` deleted), and cancel no longer hard-removes from `/booked-hotels`. Phase 3 is left as written but is partly invalidated — its `ReplaceHotelContext` (old `checkIn` + `now` "for the cancel gate") must be re-derived before building, and the replaced booking now *is* renderable on `/booked-hotels`. Rationale settled in `DecisionsToReview.md` S3/S5. |
| **Event occurrence timestamps** | `EventOccurrenceTimestampsPlan.md` | `open` — defect found 2026-08-27 while extracting the Book Flight event-model chapter, nothing built. `BookedFlightsProjector:42,47` build the inline change history from `storedEvent.timestamp()` — the event-store envelope's `recorded_at` — and render it as **"Booked on …" / "changed on …"**. Two faults: **provenance** (per Verraes, *Multi-temporal Events*, the envelope is metadata for ordering; a time the domain means is a payload property named for its purpose, filled by the producer) and, worse, **the label lies** — the flight was booked with the airline days before Ted typed it in, so this is data-entry time wearing a booking-time label, Verraes's own `reported_at` vs `report_entered_at`. **Decided (Ted, 2026-08-27): payload field `recordedAt`, UI relabelled "Recorded on"** — no new form field, because a true user-supplied `bookedAt` is one more thing to fill in on every booking and usually guessed. **Contained:** these are the only display-side envelope reads in `src/main`; `BackupService`'s use is correct (event-oriented backup writes rows verbatim) and stays. **Blocker found before planning:** `EventUpcaster.upcast(ObjectNode, String)` receives the payload only, so no rung can reach `recorded_at` — the one honest backfill source for historical events. The timestamp *is* in scope at all three call sites (`PostgresPersister.StoredEventRow.toStoredEvent`, `LegacyEventMigration`, `BackupService`), so widening the interface is local, not viral; pass a bare `Instant`, not an envelope record (second-user rule). Rejected alternatives: an optional field with an envelope fallback (leaves the defect in the projector permanently) and a bespoke migration (the read path still needs the rung, so it is the same work plus steps). Five slices: payload field on `FlightBooked`/`FlightChanged` captured from the injected `Clock` at the boundary; widen the ladder; schema bump + `FlightRecordedAtUpcaster` (v2→v3, idempotent) + golden sample; projector and renderer; backup/restore verification. **Full-snapshot trap, inverted** — `FlightChanged` is a full snapshot but `recordedAt` must **not** round-trip through the change form, it is captured fresh each time. **Open decision:** flights only (recommended) or every event type, which is what Verraes advises for decoupling but buys nothing until a second projector grows a history. **Rules landed ahead of the code (2026-08-27):** `EventSourcingRulesHeuristics.md` gained **R10** (every field a read model displays must come from an event — the mirror of R8, and what found this) and **R11** (a time a user reads is an event field, never the envelope). R11's enforcement is slice 4: `NoEnvelopeTimestampReadsTest`, a plain source scan banning `StoredEvent.timestamp()` in `application` and `web` with `BackupService` exempt, in the style of `NoAmbientClockReadsTest`. |
| **Time-ordered calendar days** | `Future_Feature_Slices.md` | `open` — deferred by Ted 2026-08-20, on seeing a 3:55 PM flight render *above* the 1:00 PM ground transfer that fed it. **The lanes stay as they are**; this records what changing it costs. The calendar is not time-ordered within a day and cannot be without abandoning its layout model: each week is a grid of fixed per-kind **lane bands** in `EntryKind` order, so an entry sits in its kind's band regardless of when it happens (the same reason a 9 PM gathering renders above a 7 AM flight). **Reordering `EntryKind` does not fix it** — a ground transfer runs both ways, so above-FLIGHT reads right for the taxi *to* the airport and backwards for the taxi *from* it; the question is about time, not kind. The **itinerary already sorts across kinds by `anchorTime`** (pinned by `aTransferToTheAirportSortsAboveTheLaterFlightItFeeds`), so the time-ordered view exists — the calendar just cannot join in. Hard part is **multi-day entries**, which are why lanes exist: a week-spanning conference or stay has no single row it can occupy without colliding with each day's timed entries, so it likely needs its own band anyway — lanes split by *duration* rather than by kind. Most of `CalendarViewBuilder.renderWeek` (`byKind`, sub-row allocation, `kindOffset`) goes, and `CalendarViewBuilderTest`'s stacking coverage gets rewritten rather than extended; kind colours, CSS classes and continuation arrows are unaffected. Natural companion to the `EntryDetails` refactor in `RendererVsProjectorResponsibilities.md`, which already touches `CalendarEntry` and the builder. |
| **Undo Cancel Hotel Booking** | `Future_Feature_Slices.md` | `open` — added 2026-08-13, nothing built. Reinstates a cancelled stay under its original `HotelBookingId` instead of forcing a re-entry. **Direct consequence of removing the check-in gate on Cancel Hotel** (same day): with no gate, the thing that keeps a mistaken cancel harmless is a cheap reversal, and today there isn't one. The greyed-out "Canceled" row now on `/booked-hotels` is deliberately action-free because Undo is the action that belongs there. Has to reinstate into *every* read model that drops the booking (calendar, itinerary, schedule problems, both tentative-hotel projectors, hotel details) — mirror each case in `HotelCancellationPropagationTest`. Shares its reinstate machinery with Phase 3 of `HotelCancelReplacePlan.md`. |
| **`ConferenceCancelled`** | `Future_Feature_Slices.md` | `open` — also the prerequisite for any slice that retracts a booking. Gathering cancellation is the same gap (explicitly out of scope in `archived/ChangeGatheringPlan.md`). The event record and the projector branches already exist (`MigrateConferenceToGathering` emits it); what is missing is an owner-facing cancel action. Note it means *organizers cancelled* — `archived/ConferenceSubmissionTrackingPlan.md` adds `ConferenceAttendanceDeclined` for "Ted decided not to go", which is a different fact — **that decline slice shipped 2026-08-16** (event + command + projectors + a Decline action on `/conferences`); the organizer-facing *cancel* action described in this row is still open. |
| **Scheduled transit trip (bus/train)** | `ScheduledTransitTripPlan.md` | `open` — planned 2026-08-23 (Ted), nothing built. A train trip learns whether it is a train or a **bus**: one enum `TransitMode { TRAIN, BUS }` on `TrainBooked`/`TrainChanged`, and that is the entire model change. **The line it draws is "does it have a schedule I have to catch?"** — a ticketed, sparse airport bus is a scheduled leg with times that can be missed and a connection worth checking; a tube ride or a taxi stays a ground transfer. Which side a ride falls on is Ted's judgment at entry, never a frequency test in code. **Cheap by construction:** `TRAIN` is the default *and* the backfill (`null → TRAIN`, so every stored row and old backup is a train, which is true), so there is **no schema bump, no upcaster, no migration, no one-off task** — if an `infrastructure` edit appears, something stopped being additive. **Decided and closed:** no renames (`Train*` is 26 types / 55 files, and the stored logical name has to stay `TrainBooked` regardless, so a rename buys a permanent mismatch rather than removing one); `/booked-trains` keeps its URL, so no `SecurityConfig` matcher and no `AuthorizationMatrixTest` row; **bus-vs-train is public** on the anonymous calendar, publishable because it adds nothing to the cities and day column already there — station names and `serviceId` stay private, and the existing absence assertions get re-run with a `BUS` fixture. `EntryKind` stays `TRAIN` (a `BUS` kind would mean a new lane, a new public branch and a new every-kind fixture row to say what the icon says). **Works unofficially today:** `TrainBooked` is already the shape of a scheduled bus, so recording one as a train trip gives real times, the DEN connection check, and a `train:<tripId>:departure` endpoint for the ground transfer that feeds it — the only thing wrong is that it says *Train*. Deferred: `FERRY` (one constant), per-mode colour, and the `Transit*` rename. Note it reverses `archived/GroundTransferPlan.md` D7 in spirit but not in shape — that doc is stale on the point. |
| **Change Private Event (cancel first, then edit)** | `ChangePrivateEventPlan.md` | `partial` — planned 2026-08-24 (Ted); **slice 1 (Cancel) SHIPPED the same day**, slice 2 (the edit flow) unbuilt. Shipped: `PrivateEventCancelled(id, reason)` + `CancelPrivateEventCommand`/`Context` + `PrivateEventNotFound`; `CancelPrivateEvent` folding existence from the event stream; a `PrivateEventDetailsViewProjector` behind the confirmation page; removal branches in all four existing consumers (owner calendar, **public calendar**, itinerary, schedule gap) plus the new one; a `privateEventId` on `PrivateEventItineraryEntry`; the OWNER-only bin on the itinerary card; `/planned-private-events/{id}/cancel` (GET confirm + POST) with its `SecurityConfig` matcher and matrix row. Suite green both tiers (1724 + 61), all new tests mutation-verified in three rounds. Takes over the "Change Private Event (the edit flow)" line from `Cleanup_Tasks.md` and **puts cancel in front of it**: a private event has no booking, so the common correction is removing a wrong entry, and a wrong one is an `Occupancy` in `ScheduleGapProjector` asserting Ted is in a city he is not — a false presence fact with no way out today. Slice 1 is `CancelGroundTransfer` with the types swapped: `PrivateEventCancelled(PrivateEventId, String reason)`, hard removal, existence the only refusal and **no time gate** (a past event is the one most worth removing), folded from the event stream not a read model. **It carries an optional reason, reversing the transfer's "no booking, nothing to explain" call** (Ted, 2026-08-24) — "better to have it and not use it", and *"rescheduled to Friday"* is the note that decides it; `HotelBookingCancelled`'s shape exactly (`""` when none), read only in `/admin/eventlog` because the removal is hard, and nothing keys off it. **The link is the itinerary's** (`cancelBin` in the title slot, the pencil's slot on kinds that have one) and OWNER-only — family can never cancel, so it renders *not at all* for them, per the authorization half of the affordance rule. Its own confirmation page, plain confirm, **no typed word, amber not red**: a `*Cancelled` after a `*Planned` destroys nothing and planning it again puts it back. **URLs take the gathering's shape, not the transfer's** — `/planned-private-events/{id}/cancel` now, `/planned-private-events/{id}` for the edit page in slice 2, leaving `/planned-private-events` free for the list view; the transfer chose its own prefix only because it has no list. Additive: new event type at `schema_version` 1, **no schema bump, no upcaster, no migration**, backup stays v3. Slice 1 also builds what slice 2 reuses — the details projector, the matcher, the matrix row, and a `privateEventId` on `PrivateEventItineraryEntry`. Slice 2 is `archived/ChangeGatheringPlan.md` minus `speaking`/`infoUrl` (`PrivateEventChanged` full snapshot, same id) and brings the pencil to both calendar and itinerary, plus **both tiers of redaction test** because it adds an event `PublicCalendarProjector` reads. **All four open questions were answered by Ted the same day, so it is ready to build as written:** amber and the no-time-gate both stand (a cancelled *past* event cannot be re-planned — accepted, rather than colouring the page from the event's date); the **calendar's** cancel bin waits for slice 2 so the card gains pencil and bin together rather than one action at a time; cancel **records a reason** after all; and `VenueEventRequest` stays unextracted — a third copy of `getLocation()` is the same case seen three times, not a shape proved general, so it waits for a fourth or a different-shaped user. |
| **`/planned-private-events` list view** | `PlannedPrivateEventsListPlan.md` | `shipped` — planned and built 2026-08-24 (Ted). The private event was the only planned kind with no list page, and the gap was sharper than "one more list": `PrivateEventPlanned` carries a full `Address`, and **`street`, `region` and `postalCode` were read by no view at all** — typed into the plan form, written to the log, never displayed. Shipped: `PlannedPrivateEventView` (`TemporalView`, `relevantUntil() = endsAt.utc()`), `PlannedPrivateEventsProjector` (plan → put, cancel → hard removal), `PlannedPrivateEventsRenderer` (four columns — When / Private Event / Venue / actions, the gathering table minus its Speaking column), `PlannedPrivateEventsController`, the `SecurityConfig` matcher + matrix row, a "Private Events" link in the OWNER nav bar and a nav card on `index.html`. **Cancel is the row's only action and renders first in the cell** so slice 2's `Edit` appends *below* it and nothing that is there today moves. **The cancel page's keep-link now returns here** (`Keep it — back to the list` → `/planned-private-events`, copying `decline-conference.html`), leaving the flow deliberately asymmetric: keeping returns to the list, cancelling still redirects to `/itinerary?date=…`. Icon is FA Pro `utensils`, the same SVG on both private-event cards — which **closes the placeholder-icon line** in `Cleanup_Tasks.md`, the placeholder having turned out to be the right icon. Deliberately *not* a widened `PrivateEventDetailsView`: that record is the cancel page's, and a recording surface carries identification only. Suite green both tiers; all new assertions mutation-verified in three rounds. |
| **`mapsUrl` on conferences** | `Future_Feature_Slices.md` | `open` — auto-computed from venue + address, as hotels do. |
| **Near-name check on Plan Conference** | `Future_Feature_Slices.md` | `open` — requested by Ted 2026-09-07, nothing built. **Warn and proceed, never block**: `/plan-conference` shows conferences whose name is close to the one being typed, with dates, city and a link to each, and the next submit goes through. A similar name is *not* a duplicate — there are several regional Devoxx events, and "Devoxx UK" must not be refused because "Devoxx Belgium" exists. Nothing checks today: `PlanConferenceCommand` validates only its dates. The search must span **every** conference including past and **dropped** ones, since a declined conference is invisible on `/conferences` by default and is therefore the one most likely re-added. The matching rule is the risk and gets **validated against the production backup before shipping**, on the `EnteredLocation` precedent — and when it misfires, shorten it rather than weaken it. Four open questions in the doc (what counts as "near", whether date proximity matters, how the acknowledged second submit is carried without losing typed values, and whether gatherings/private events get it too). Split from the same request's *other* half — a way to find an already-added conference **from `/calendar`**, which is still an open question with no shape yet. |
| **Sunday/Monday week start** | `Sunday-Monday-Week-Start-Switch.md` | `open` — not started. `CalendarViewBuilder` still hardcodes Sunday (`CalendarViewBuilder.java:58`, `:59`, `:64`). Contained to the `web` package. |
| **Viewer-timezone selection** | `Future_Feature_Slices.md` | `partial` — re-read 2026-08-16 against current behavior. Two of the three pieces this asks for now exist: entry times localize to the browser zone (`BrowserZoneScript`, `archived/UtcDatetimeStoragePlan.md` phase 4) with a `ZoneToggle`, **and** the calendar/itinerary notion of *today* is now viewer-zoned via a `viewerZone` cookie captured at login (`0435623`, decision 10 of `UtcDatetimeStoragePlan.md` — see the Shipped table). **Still unbuilt:** the explicit **per-view zone-override dropdown** this doc describes (choose an arbitrary zone on the Calendar/Itinerary, defaulting to the browser's). What exists is a two-state entry-zone/browser-zone toggle, not a free zone picker. Only the dropdown is left. |
| **Spring Boot 4.1.1 upgrade** | `SpringBoot41UpgradePlan.md` | `open, agreed, HELD` — **held by Ted 2026-09-06: the version bump does not happen until he says go.** **S1 is done** (2026-09-07): Cancel Train landed in `0e0a0c7`/`846ee9d` and the tree is clean, which cleared the *precondition* and not the hold. **Three of the four questions are now answered: Modulith 2.1.1 confirmed, the Dockerfile `-DskipTests` → `-Dmaven.test.skip=true` change taken** (both Ted, 2026-09-06)**, and the equivalence test retired ahead of the bump** (2026-09-07, below) (inert today, so it cannot cause a red, and keeping it in the upgrade commit is what makes the whole thing one revertable change; note the flags are not synonyms — `maven.test.skip` skips test *compilation* too, which is right for an image that never runs tests). The one left open is genuinely not answerable before the suite runs. S1 *was* the blocker — the tree carried a large staged Cancel Train / overlapping-legs feature (~70 files) touching `SecurityConfig`, `AuthorizationMatrixTest`, `CalendarRedactionSecurityTest` and `GoldenEventDeserializationTest`, the exact four the upgrade is judged by, so a red in a mixed tree could not have been attributed to either change; it has landed. Planned 2026-09-06, reviewed against the tree 2026-09-07 against the 4.1 release notes, the 4.1.0 configuration changelog and the 4.2.0-M2 notes read ahead; **nothing changed yet**. The change itself is **two numbers in `pom.xml`**: parent 4.0.7 → 4.1.1 and `spring-modulith.version` 2.0.6 → 2.1.1 (`spring-modulith-starter-core:2.1.1` declares `spring-boot-starter:4.1.1`; the 2.0.x line is built against Boot 4.0, and 2.0.6 is two patches stale inside its own line). **Almost none of the 4.1 notes are ours** — no Derby, no layertools, no JPA, no gRPC/Rabbit/Kafka/JMS/Redis/LDAP/Batch/jOOQ, and none of the seven properties removed in 4.1.0 appear anywhere in `src/`. **Two things carry real risk.** (1) **Spring Security 7.0.6 → 7.1.1** — a whole Security minor rides inside the Boot minor, and `SecurityConfig` is the threat model in one file; 7.1 documents no breaking change to anything we use (`authorizeHttpRequests`, `CookieCsrfTokenRepository` + cookie customizer, custom `formLogin`/`accessDeniedHandler`, `InMemoryUserDetailsManager`, BCrypt), but that is a documentation claim and `AuthorizationMatrixTest` / `SecurityAuthorizationTest` / `CalendarRedactionSecurityTest` are the proof — a red there is a **finding**, not test maintenance. (2) **The auto-configured Jackson mapper changes, and that no longer costs us a test**: Boot 4.1 changes it (`spring.jackson.read.*`/`write.*`, `spring.jackson.factory.*` read constraints, a bean-aware `HandlerInstantiator`), which is precisely the mapper `EventJsonMapperEquivalenceTest` pinned ours against — **which is why that test was retired ahead of the bump** (`6d25065`, 2026-09-07), its one-time migration proof spent, rather than left to go red mid-upgrade and be argued out of being a failure. **Production serialization is insulated by construction** because `EventSourcingConfig` pins the bean to `EventJsonMapperFactory.create()`, so the fix is *never* to edit that factory. The durable contract is `GoldenEventDeserializationTest` + `RestoreSafetyTest` + `BackupRestoreRoundTripTest`, and a red *there* blocks the upgrade outright. **The gate is S7, not the deploy:** `./mvnw test -Preplay-preflight -Dpreflight.dump=<production backup>` replays the whole stored log under the new Jackson — the golden samples are a handful of shapes. Also checked: a `showDeprecation` build on 4.0.7 reports **four** warnings, all in tests and none from Boot (`JsonNode.asText()` ×3, `HttpStatus.UNPROCESSABLE_ENTITY`), so 4.1's removal sweep has nothing to remove from us; no `logback-spring.xml` and no Janino; thymeleaf-layout-dialect 3.4.0 → 4.0.1 is **unused**. **4.2 read ahead and it costs this app nothing** (its two breaking changes are embedded-LDAP SSL bundles and Janino), so there is no reason to wait for it — and no reason to jump to 4.2.0-M1, which would mean two pre-release lines under a production event log. Rollback is `git revert` of one commit: nothing here writes to the database, changes an event's shape, or migrates a row. **Four open questions for Ted** in the doc: Modulith 2.1.1 confirmation, what happens to the equivalence test if it goes red (recommendation: retire it — its claim was a one-time migration proof), whether the two spike test deps (`strictland`, `spring-test-profiler` — neither managed by Boot) get dropped or fixed if they break, and whether to take the `-DskipTests` → `-Dmaven.test.skip=true` Dockerfile hardening now (inert today; AOT stopped honouring `-DskipTests` in 4.1). |

## Open cleanups

**Owned entirely by `Cleanup_Tasks.md`; there is no roll-call here any more** (2026-09-08 — see the
rule in this file's header). That file's three sections answer the three questions the table used to
try to, and answer them in more detail than a row can hold:

- **Open** — small work that is wanted. It includes the two `locationForMatching` items, which are
  waiting on **Ted's decision** rather than on code; the table used to flag that with a `decision`
  status, and both items now carry the question in their own titles instead ("ask whether that is
  still a problem", "may be droppable").
- **Deferred (until needed)** — known shape, named trigger, deliberately not queued. Do not promote
  one without its trigger firing.
- **Done** — the record.

Larger structural duplication has its own doc: `Refactoring_Opportunities.md`.

## Decisions waiting on Ted

| Decision | Owning doc |
|---|---|
| **Adopt Strictland, or revert the spike?** The mapper pin (`EventJsonMapperFactory`) stays either way. Adoption is paused pending the author's feedback. If adopted: pick the first events to cover, the snapshot-file layout, and how coverage is enforced for new event types. | `Event_Serialization_Contract_Tests.md` |
| ~~**Alpha-2 zone aliases for single-zone countries** (e.g. `GB`)~~ — **closed 2026-09-07, not built** (Ted). The premise was wrong: no alpha-2 code appears in any of the eight production backups, and `AddressParseService` reads Nominatim's spelled-out `country`, never its `country_code`, so no automated path can produce one. `UK`/`USA` are Ted's own typing and both already resolve. A hand-typed `GB` throws and re-prompts for a `CommonZone` — the strict-no-default design working. Evidence and the collision analysis are in the archived doc; the *real* problem in that area is now the location/address validation item in `Cleanup_Tasks.md`. | `archived/UtcDatetimeStoragePlan.md` (improvement 2) |

## Explorations — deliberately unbuilt

These are design records. Do not implement without discussing with Ted first; several say so
explicitly in their own text.

| Doc | What it is |
|---|---|
| `CommandConsistencyEventStore.md` | Conditional/fenced append for a future multi-instance deployment. Nothing built. Unblocked by the CommandExecutor migration, but not committed to. |
| `DataStarResearchPlan.md` | `exploration 2026-08-20` — replace the 8 inline-JS UI behaviors (dropdown prefill, toggles, address fetch-and-fill) with Datastar? Recommendation: **not now** — free tier fits only 3 of 8 (the largest, the browser-zone script, needs paid Pro), tests move rather than disappear, and it adds 1–2 dependencies. Revisit triggers: a slice needs SSE server-push, cascading form interactivity, or the inline-script count doubles. Pilot order if adopted: address paste-parse → ground-transfer prefill (backend-driven) → calendar toggle. |
| `DecisionContextQueryDesign.md` | `unblocked — ready to build` (was `paused`) — replace `CommandExecutor.eventsForDecision()` and the projector-based existence checks with a tagged/typed query port. Design close to settled (recommendations + open decisions recorded); the export/import blocker is resolved by `archived/EventOrientedBackupRestorePlan.md` (2026-08-10). Owns `DecisionsToReview.md` D1 and D2. |
| `TaggedEventStoreQueryingDesign.md` (**repo root**, not `docs/`) | Filtered `EventStore` queries by event type and tag, pushed into Postgres JSONB + GIN. Deferred until a second concrete caller demands it. Its `@EventName` proposal is now **obsolete** — `EventTypes` does that job. Its "every id is a tag" rule and multi-valued tag shape are live input to `DecisionContextQueryDesign.md`. |
| `ReMoDeL-Specification.md` | A KDL-based read model definition language. Specification only — there is no KDL code anywhere in `src/`. |
| `ReadModelKdlTestDslPlan.md` | Test-support DSL for the above. Its 8 implementation steps reference an acceptance test that does not exist yet. Blocked on ReMoDeL itself. |
| `RestoreCompatibilityFloorPlan.md` | `design 2026-08-19, nothing built` — can restore refuse a backup that predates a **retired** upcaster rung? **It already does:** pass one's climb fails loud, so the file is rejected with zero writes and every problem reported, and `/admin/restore/validate` is the dry run. What is missing is *diagnosis* — the refusal reads as ~200 lines of per-event "payload cannot be restored" instead of "this file is too old". Proposes a per-type `minimumRestorableSchemaVersion` in `EventTypes` (bumped in the same commit that deletes a rung) plus one aggregated verdict naming the count, an example row and the remedy. Format-version bump, a global minimum, a date-based floor and auto-upgrade were all rejected, with reasons. **Build trigger: the first rung retirement, same commit** — every floor is 1 until then. Blocked on the undrawn retirement date (`MigrationLessonsLearned.md` follow-up 3). One test in it is worth doing now regardless: assemble a ladder with a rung omitted and pin the fail-loud message. |
| `Refactoring_Opportunities.md` | 7 projection/rendering duplication findings with a priority order. Marked "do not implement without discussion". |
| `RendererVsProjectorResponsibilities.md` | `discussion` — what is a renderer's job vs. a projector's, and which should a controller depend on? Opened 2026-08-18 out of the reverted state-aware nav link (seven controllers gained a `ScheduleGapProjector` for one nav fact). Four options sketched (status quo / renderer-holds-projector / renderer-as-instance / a page-model layer); nothing decided. Ted wants to pick this up later. Concrete trigger to revisit: the first time a renderer needs a real collaborator. **Reopened 2026-08-19** on a second, different occasion — `CalendarEntry` growing a `commitment` field that applies to one `EntryKind` only, at the start of slice 2 of `archived/ConferenceSubmissionTrackingPlan.md`. That section separates two problems (record shape vs. redaction-after-the-fact), sketches E1–E4 (shape), M1–M2 (how much a projector may render), S1–S2 (per-audience projection, which would delete `CalendarEntryRedactor`), a precise change list for S2, and answers a side question on `EventStreamConsumer` advertising event interest (recommendation: don't). **DECIDED 2026-08-19: S2 + E2** — one `PublicCalendarProjector` (deletes `CalendarEntryRedactor`), core `CalendarEntry` + sealed `EntryDetails`, `kind()` derived from `details` so the two hierarchies can't drift; E1 lost because the public side must collapse many private kinds to one "Busy" shape and its security invariant would grow with every kind. **Commit split decided 2026-08-21: E2 first**, so the big mechanical diff lands while the redactor's compile-time forcing function is still armed, and the security change is then a small focused diff. **Commit 1 (E2) SHIPPED 2026-08-21**: `CalendarEntry` went from **13 fields + 4 convenience constructors** (worse than this doc recorded — ground transfer had added `publicRoute` and `cancelPath`) to **7 fields + 1**, with a sealed `EntryDetails` holding everything kind-specific and `kind()` derived from it. The redactor now switches on the details type (exhaustive over the sealed type, so the forcing function is per-kind and sharper); `CalendarViewBuilder` gained three exhaustive switches (`titleLink`/`ownerActions`/`badges`) in place of four flat field reads; the gathering's link is now honestly named `infoUrl`. Two redaction tests were **deleted as unwriteable** — including the five-kind commitment loop, replaced by `redactionNeverChangesTheKind`, which does not grow with the kinds. Both tiers green (1343 + 50), six mutations verified. **Commit 2 (S2) SHIPPED 2026-08-21**: `CalendarEntryRedactor` and its test are **deleted**; one `PublicCalendarProjector` handles all 16 event types and emits public entries directly, `CalendarController` picks the read model by audience, `CalendarRenderer` strips nothing. The term-3/term-4 tension (kind derived vs. public side collapsing) resolved via `EntryDetails.PublishableTravel`, a sealed interface over four empty records — nothing stored, one switch arm. `Busy` collapses the *lane* too, so a future private kind cannot be told apart from a dinner by lane alone (now a CLAUDE.md rule). The invariant is both a compiler check (the projector's `entry(...)` helper takes a `Publishable`) and a test that names no type list. `publicRoute` deleted. `CalendarRedactionSecurityTest` is now explicitly the primary guard and builds its anonymous fixtures from **real events through a real projector**; `CalendarRendererTest`'s redaction test was replaced by one asserting the renderer strips nothing. The one-off equivalence check (both pipelines → identical rendered anonymous page) caught 2 of 2 mutations, then was deleted with the redactor; 3 further mutations verified against the permanent tests. Both tiers green (1346 + 50). CLAUDE.md's redaction section rewritten for the new mechanism. The *original* renderer-vs-projector question (options A–D) remains undecided. |
| `authorization_policy_centralization.md` | Authorization "Option 2" — derive `SecurityConfig` matchers and `GeneralController` nav flags from one `NavArea` policy type. Current Option 1 (two small hand-synced places) is judged low-risk because `AuthorizationMatrixTest` catches drift. This doc is the live tracker; the duplicate checklist inside `archived/GeneralControllerRefactorPlan.md` is not maintained. |

## Done

Kept as design records. Each has a status banner at the top of the file.

**One line each, on purpose.** A finished row says what shipped and what the doc is worth keeping
*for* — the reasoning that outlived the work, and any rule of thumb it argued out. The detail lives
in the owning doc; repeating it here made "Open features" a place finished work went to be
described, which is the opposite of what that table is for.

**Most of these live in `docs/archived/`** — see `archived/README.md`. Nothing was deleted; only
the path changed. A doc goes there when it owns no remaining work: the six that shipped with a
follow-up of their own had that follow-up **lifted into `Cleanup_Tasks.md` first**, so archiving
never drops an open item.

Rows with **no** `archived/` prefix are still in `docs/`, each for its own reason:
`EventPayloadUpcasterDesign.md` is a live how-to — how to add and retire an upcaster rung — not a
finished plan. One row has no doc at all: a finished sub-item of a plan that is still in progress,
which can never be archived on its own. (`UtcDatetimeStoragePlan.md` was the other, held back for
its one open decision; that closed 2026-09-07 and it moved to `archived/`.)

| Doc | Landed |
|---|---|
| `archived/YearOverviewPlan.md` | `2d79414`, 2026-09-01; review fixes `80da18f`/`ba1b0ad`, 2026-09-04. Worth keeping for **what a zoomed-out view drops**: seven tints and seven glyphs were built, then cut to three tints and one plane the same day, because absence only carries a signal in a sparse vocabulary (now CLAUDE.md's "Zooming out is lossy on purpose"). Also **why the sticky month bands went** — a week is filed under its Sunday, so they could not anchor a jump — and a banner listing the five plan decisions the build reversed. |
| `archived/UtcDatetimeStoragePlan.md` | All 7 phases + review fixes. `25104b9`, 2026-08-05. Held back in `docs/` until its last open decision closed; **archived 2026-09-07** when alpha-2 zone aliases were closed as *not built* — no alpha-2 code appears in any of the eight production backups, and the only automated path (Nominatim) emits spelled-out country names by construction. Worth keeping for that evidence, and for the state/province fix that made `WA` unambiguous. Its three surviving non-blocking notes were lifted to `Cleanup_Tasks.md`. |
| `archived/UtcDatetimePlanReview.md` | Folded into the plan above; historical record, checkboxes no longer maintained |
| `archived/GatheringConferenceUtcRolloutPlan.md` | Gathering `ddf4ba8` (2026-07-27), conference `67a47af` (2026-08-05) |
| `archived/TrainFlightUtcRolloutPlan.md` | Trains `daa7107`, flights `d2884fb`, 2026-06-24 |
| `archived/HomeCityPlan.md` | `26d15ac`, 2026-07-26 |
| `archived/ChangeGatheringPlan.md` | `f28cb49`, 2026-07-25 |
| `archived/ChangeHotelPlan.md` | `d98af11`, 2026-06-21 |
| `archived/ChangeTrainPlan.md` | `8390262`, 2026-06-17 |
| `archived/Remove-Local-Profile.md` | `e962915`, 2026-06-17 |
| `archived/j2html_Migration_Analysis.md` | `b0e6f11`, 2026-06-06 — views migrated, shared infrastructure not extracted (see loose follow-ups) |
| `archived/GeneralControllerRefactorPlan.md` | Shipped; its "open items" 1 and 2 are resolved, item 3 moved here |
| `archived/EventOrientedBackupRestorePlan.md` | `done 2026-08-11` — event-verbatim backup/restore replaced command replay; commands ride along as opaque history for a future undo. Read models rebuild on **restart** (the live rebuild was built then reverted — email-sender hazard). **Backup-format break to v2**; old command-only files are not read. |
| `archived/LegacyEventEagerMigrationPlan.md` | `built 2026-08-16` — eager legacy-row migration + per-event `schema_version` stamp + boot-replay preflight. |
| `archived/CalendarSubscriptionFeedPlan.md` | `done 2026-08-18` — Phase 1 (cancel-deadline reminders), validated on-device in prod. **Phase 2 (full travel calendar) lifted to `Cleanup_Tasks.md`.** |
| `archived/PrivateSocialEventPlan.md` | `done 2026-08-13` — `PRIVATE_EVENT` kind, its own redacting branch, itinerary entry. **Change + list view lifted to `Cleanup_Tasks.md`.** |
| `archived/ScheduleProblemsRewritePlan.md` | `shipped 2026-08-20` — all seven slices; detection rebuilt on one located timeline. **The mirror of D14 lifted to `Cleanup_Tasks.md`.** |
| `archived/ProblemCalendarPlan.md` | `done 2026-08-20` — slices 1–5, ending with clash markers and fix links. **One of its two open questions is answered** (the calendar is the default view, 2026-08-21); the day-number link stays in `Cleanup_Tasks.md`. Two of its decisions are annotated as reversed there: F9's one-control-per-card menu, and F1's per-kind band fills. |
| `archived/GroundTransferPlan.md` | `done 2026-08-20` — plan + cancel. Change was already tracked in `Cleanup_Tasks.md` (Deferred). |
| `archived/CalendarAwayBandPlan.md` | `done 2026-08-21` (`d4a9a3f`) — turquoise away stripe on `/calendar`. No tail: the schedule-problems variant was **closed** by Ted 2026-08-21, not deferred. |
| `archived/EventTypeColumnNormalizationPlan.md` | built 2026-08-19, **run in production 2026-08-21** — `event_log.type` now holds one spelling per type. The `EventTypes` aliases **stay** (append-only; a pre-normalization backup still restores the old names). Two unconfirmed post-click runbook steps → `Cleanup_Tasks.md`. Keeps the rollback analysis, which is the reason the runbook says to take a backup **first**. |
| `archived/ConferenceSubmissionTrackingPlan.md` | `done 2026-08-23` — all five slices: `ConferenceFormat` (2026-08-18), the commitment fold and "Maybe" chip (08-19), `CfpOpened` + the CFP-deadline iCal source + the `/conferences` dashboard (08-21), the submission stream and the public speaking badge (08-22), and the gap projector dropping a dropped conference (08-23). Splits "tentative" into **two dimensions** — attendance commitment and a speaking pipeline — both **derived**, never stored. **The doc to read before touching conference redaction:** why commitment is publishable (every speculative state collapses to one `WATCHING`, so the chip cannot be mapped back to a submission outcome) and why the basis, CFP dates and talk titles are not. Also keeps the "no waitlisting → last event wins" correction, the state-machine-beats-affordances nuance, and slice 5's collision with the commitment-blind away band. Its one named gap — **no way to change a conference** — went to `Cleanup_Tasks.md` (Deferred). |
| `archived/QuietWeekRunsPlan.md` | `exploration 2026-08-31` — **built, refined through three visual iterations, then reverted the same day**: it solved "the linear calendar is too long", which Ted identified as a symptom, not the problem (`"then the gaps are irrelevant"`). Superseded by `archived/YearOverviewPlan.md`. Its sticky month band was re-landed alone, then
**removed again 2026-09-01** when the year overview shipped — a week is filed under its Sunday, so a
band could not anchor a jump and Sep 1–5 sat under one reading "AUGUST 2026". Nothing left to
re-land. Kept for **D4**, the away-day trap (an empty week can still be a *banded* week — any future feature reading "no entries" as "nothing here" hits this), and **D5**, three failed attempts at making one element visible and what finally worked. Its sticky month band → `Cleanup_Tasks.md` (Open), worth re-landing alone. Also keeps **why the strip does not expand** (it hides only absence, so an expand reveals nothing — the past-weeks toggle works because it hides *entries*), why the horizon is where Ted stops planning by tapping a day, and **D4**: an empty week can still be an away week, because `walk()` fills the nights *between* points, so a trip with no hotel yet bands weeks holding no entries. Also records three rejected alternatives, including why "collapse empty *months*" cannot be built on a week grid. Two follow-ups → `Cleanup_Tasks.md` (Deferred), one of them the **zoomed-out year view**. |
| `archived/SessionizePrefillPlan.md` | `done 2026-08-23` — paste a Sessionize URL on `/plan-conference` and fill the form: the deadline exactly, from the `.ics`; everything else scraped by regex, best-effort. Keeps **why regex and not jsoup**, why the planned two-slice split had to be abandoned (the deadline field is read in the venue zone, which only the scrape supplies), and the one place here where **blank beats a defensible value**. No tail. |
| `archived/ProblemContextOnFixPagesPlan.md` | `done 2026-08-21` — a fix link now lands on a form that says *why*, via `?problem=<key>` resolved against the live report. Keeps the reference-not-the-words rule (a URL can lie and can go stale; a lookup cannot), the reuse-the-wording rule, and the `@ControllerAdvice` decision. Its one named follow-up, ground-transfer endpoint prefill, was lifted to `Cleanup_Tasks.md` (Deferred) when it was archived 2026-08-23. |
| — (no doc of its own) | **Public "speaking" badge on the calendar**, `done 2026-08-22` — gatherings half 2026-08-17, conferences half with slice 4. That Ted is speaking is never private (the place and time already are), so the "A Ted Talk" badge renders for anonymous viewers. Two conditions on the conference half, both load-bearing and both written up in `CLAUDE.md`: it is published only for a conference he is **committed** to, and `ConferenceProgress.speaking()` is the single place that decides. Detail in `archived/ConferenceSubmissionTrackingPlan.md`. |
| — (no doc of its own) | `infoUrl` on conferences, `done 2026-08-22` — shipped inside conference slice 4b; the detail is in the **Shipped without an owning plan doc** section below, and the owning plan is still in progress. |
| `archived/GroundTransferEndpointReadModelPlan.md` | `done 2026-08-23` — all three slices. **The bug it was written for is fixed:** a missing-travel gap that starts or ends at a **train station** now has the station to pick, and preselects it. Worth keeping for three arguments. (1) **`Place`** — two readers had to agree on which field of an event becomes a place, or preselection silently stops matching and a submitted transfer *removes* the gap it was entered to close; that is a missing value type, not a missing test. (2) **Read models are per-view** — the form's endpoints are their own projector built from events, not two list views converted into a third, which is why a cancelled stay is now absent rather than filtered and why trains cost one arm instead of a third set of accessors. (3) **What is a fact about the event vs what needs `now`** — the end an endpoint can serve, its place and its moments live in the row; the day filter, the ordering and the label stay in `GroundTransferEndpointOptions`. Also records the pairs that look like duplication and are not (`city` vs `place`, `moment` vs `offeredUntil`), the one deliberate behaviour change (ordering by instant, not local wall clock — they differ only across zones and there the clock is wrong), and why trains read their zone off the event where hotels still re-derive theirs. **Four tails lifted to `Cleanup_Tasks.md` (Deferred):** the hotel zone divergence, venues as endpoints, reshaping `GroundTransferEndpointChoices` around `TransferEnd`, and carrying `Place` deeper. |
| `archived/CuratedResolversToDomainPlan.md` | `done 2026-08-23` — `AirportCityResolver` + `StaticAirportCityResolver`, `AirportZoneResolver`, `LocationZoneResolver` and `ZoneResolutionException` moved to `domain`; imports updated across 86 files, three test classes moved with their subjects, nothing else changed. The rule it argued out is now a standing section in `CLAUDE.md` ("What belongs in `domain` — and what a curated table is"), which cites this doc by path for the reasoning. The reasoning worth keeping: **a curated in-memory table is data, not I/O**, and an interface kept as a seam for a future non-static lookup is ordinary ports-and-adapters, so neither disqualifies a type from `domain`. New `DomainIsPureTest` enforces the rule as a **whitelist of two import prefixes** (`java.*`, own package) rather than a blacklist of the libraries we happen to use — which is what makes a dependency nobody has thought of fail on arrival. `UUID` is exempt by standing decision (Ted, 2026-08-23): the seven `*Id.random()` factories stay, and the test pins the live half instead — **zero** `*Id.random()` call sites in `src/main`, 407 in tests, because production mints ids at the boundary. Also records why finding 1 (`Address`'s Jackson `@JsonAlias("state")`) was **retired rather than relocated** the same day, on the measurement that no restorable artifact has ever carried the old spelling. 1628 unit + 61 js green on a `clean` run, all three new guards mutation-verified. Its one named tail — the matching `setState` shims on `BookHotelRequest`/`ChangeHotelRequest` — was **lifted to `Cleanup_Tasks.md` (Open)** the same day. |
| `EventPayloadUpcasterDesign.md` | Reference doc for the read-time version-ladder. The upcaster framework `archived/LegacyEventEagerMigrationPlan.md` deferred, now built (2026-08-18): `EventPayloadUpcaster` is a composite of per-`(type × step)` `EventUpcaster` rungs. How-to-add / how-to-retire a rung lives here. |

## Shipped without an owning plan doc

Small-to-medium slices that went straight from conversation to code, so no plan doc names them.
Recorded here only so this index matches the tree — none of them has open work unless a row says so.

| What shipped | Landed |
|---|---|
| **R10–R12 and the read-model dependency guard.** `EventSourcingRulesHeuristics.md` gained R10 (every field a read model displays comes from an event), R11 (a displayed time is a payload field, never the store's envelope) and **R12** (a read model is built from events alone, never from another read model — compose one layer up, in an aggregator or a controller). R12 is enforced by `ProjectorsDependOnEventsAloneTest`, a plain source scan over all 27 `*Projector` classes with comments and string literals blanked first, so a comment may name another projector while code may not. **Its limit is a naming convention**: it keys on the `*Projector`/`*Aggregator` suffix, so a read model behind an interface named otherwise is invisible to it. R11 is a rule ahead of its code — its enforcement is slice 4 of `EventOccurrenceTimestampsPlan.md`, which is still open. | `45048c7`, 2026-09-01; positive-case guard `80da18f`, 2026-09-04 |
| **Conference slice 4b — where the talk stands, the two URLs, and the CFP on the plan form.** Prompted by Ted recording a `Rejected` and finding the row still showing the CFP deadline — the wrong fact for a conference that turned the talk down, and worse, the thing that made the `Decide` group's two situations (rejected, and CFP-closed-with-nothing-submitted) render identically. **The line under the name now says where the talk stands** — `Talk submitted` / `Talk accepted` / `Talk rejected` / `Invited to speak` — and stays the deadline, link and all, only in the two states where submitting is still on the table (`NOT_SPEAKING`, `WITHDRAWN`). Ted chose the full mapping over "rejected only" because it also makes `Going` distinguish an accepted talk from an invitation taken up from a ticket bought. **`infoUrl` shipped on `ConferencePlanned`** (public, per CLAUDE.md — `EntryDetails.Conference` *and* `EntryDetails.PublicConference`, so the title links out on both calendars, the itinerary and `/conferences`) and **`submissionUrl` on `CfpOpened`** (OWNER-only — a Sessionize link is the submission pipeline in one field; it renders as a `Submit` link beside the deadline, and goes when the deadline goes). Both are **compact-constructor-normalized `""` with no schema bump and no upcaster**, the deliberate contrast with `format`'s v2→v3 rung: a behavioural non-null field has to be stored, a missing URL has an obvious empty meaning — so every pre-existing backup still restores, pinned by `ConferenceUrlFieldCompatibilityTest`. **`/plan-conference` now records the CFP in the same submit** — this is `archived/SessionizePrefillPlan.md`'s **Slice 0**, built to spec: two commands (plan **then** CFP, order forced by `OpenCfp`'s fold), two commandIds captured at the boundary, the deadline stamped with the zone `PlanConferenceHandler` already resolved rather than a second resolution. **One correction to that spec:** both refusals moved *ahead of the first command* into `ConferencePlanning.refuseImpossibleCfp`, because refusing after the plan landed would leave a planned conference behind a re-renderable form, and re-submitting it would plan it twice; the domain's `ConferenceHasNoCfp` stays as the backstop for a direct POST, and new `CfpDeadlineMissing` (application, beside `ZoneResolutionException`) covers a submission URL with no deadline — refused rather than dropped, since a value that vanishes silently is the failure Ted would not notice. CFP fields render for every format including Open Space: hiding them as the radio changes would move every field below them, and the server refuses the combination anyway. Both tiers green (1572 + 50); nine mutations verified. **Still open, and known when `infoUrl` was placed on the plan event:** there is no *change conference* flow at all, so `infoUrl` — like the name, dates and venue — is set once; the CFP half has `/conferences/{id}/cfp` as its repair path and the conference half has none. | 2026-08-22 |
| **`/conferences` filter toolbar** (design handoff, option 2a). Replaces the "Show dropped"/"Hide dropped" link, whose label named the *action* — nothing on the page said whether the dropped conferences were currently in or out. Now one bordered bar holds a **count per section, each jumping to it** (`#section-<group>` anchors, no script) and a **checkbox-styled switch** reading `Show dropped n` in both states, with only the tick changing; `[aria-pressed]` drives both the CSS and what a screen reader announces, so the drawn state and the announced state cannot drift. Counts come from the sections that rendered, so every jump lands somewhere; the dropped count comes from the new `ConferenceProjector.droppedCount(timeView, now)`, because the switch has to say how many the page is holding back. Decisions taken with Ted 2026-08-22: **a button per non-empty section**, not only the design's four — `DashboardGroup` has eight, and `Invited` is ranked second-most-urgent, so it needed a way to be reached; **the Dates column is day-only** (`EEE MM/dd`, one range in one column) — merging Start/End is what pays for the fixed 240px Actions column, and the clock times went with them, so a conference row now carries no `<time>` element at all; **no sub-820px media query** — the elastic Name/Dates/City columns absorb the squeeze by wrapping their own content, which Ted had already tested, so `.conf-actions` stays `nowrap` at every width. Table is `table-layout: fixed` over five columns (Name / Going? / Dates / City / Actions). New CSS lives in `ConferencesRenderer.CSS` beside its siblings rather than in `site.css`, which holds the cross-page controls only. **Two follow-ups the same day (Ted):** the CFP deadline under the name now shares the Dates column's date format — one `DATE_PATTERN = "EEE M/d"` for the page, because `Wed 9/2` in the column with `Sat, Sep 12` beneath it is one row saying the same kind of thing two ways (the *time* stays: a deadline is a moment, and 11:59 PM is the point of it). **Unpadded, settled by asking:** the handoff said `MM/dd` and Ted's examples said `9/1`, and the padding got assumed rather than asked about — twice, the second time over a comma. Standing instruction from that: when an example disagrees with a named pattern, ask, never pick one and flag it. Both date tests now use a single-digit month deliberately, since only that case can tell `M/d` from `MM/dd`; and **"Plan another conference" moved from under the last table into the toolbar's right edge** via `margin-left: auto`, since at the bottom it was reachable only by scrolling past every section and grew further away the more conferences there were — the one control whose distance depended on the data. **Two more the same day (Ted):** the `Not going` chip gained `1px solid var(--border-color)` — it is the only chip whose fill is nearly the row's own colour, so with no edge it had no visible left boundary and read as misaligned beside the solid ones — with its padding cut to `1px 5px` so the outer box still matches the filled chips, the same compensation `.conf-speaker` uses (a border added on top of the shared `2px 6px` would have moved the misalignment, not fixed it); and **`Plan another conference` is now filled green (`#1e7a1e`) with bold white text**, because outlined in the accent colour it read as a third filter — accent-filled is what the active time segment is, and accent text is what the jump links are, so accent on that toolbar already means "filter". Border matches the fill so the box stays `.time-toggle`'s size. It shipped as CSS `forestgreen` first; white on that is **4.4:1, under WCAG AA's 4.5:1** for text this size, so Ted took the two-shades-darker `#1e7a1e` (5.4:1, visually the same green) and bold on top — weight being the other half of legibility on a filled control. The renderer test pins the **exact** hex rather than "some green", since a test that accepted any green would let the contrast regress silently. Both tiers green (1529 + 50); eleven mutations verified. | 2026-08-22 |
| **Viewer-zoned "today" + custom login page.** The calendar/itinerary computed *today* in the server's JVM zone (UTC on Railway), flipping a day early each Pacific evening. Now `ViewerTodayZone` resolves a `ZoneId` from a `viewerZone` cookie (else `jittertravel.today.fallback-zone`, default `America/Los_Angeles`); controllers compute `LocalDate.ofInstant(clock.instant(), zone)`. The server can't read the browser zone unaided, so a **custom login page** (`LoginController` + `templates/login.html`, replacing Spring's generated one) carries a hidden `browserZone` field JS fills, and `ZoneCapturingAuthenticationSuccessHandler` writes the cookie on the login-success response — correct on first paint even for a deep link bounced through login. Cookie is set **only** on an authenticated response, so anonymous visitors never receive one. Also: calendar default `from` is now one week before today. Documented as **decision 10** of `archived/UtcDatetimeStoragePlan.md` (a later increment to that otherwise-`done` plan). Advances the **Viewer-timezone selection** open row above (only the zone-override dropdown remains). | `0435623`, 2026-08-16 |
| **Decline Conference slice.** Ted's own "decided not to attend" (accepted to Devoxx Morocco, then withdrew), kept DISTINCT from organizer `ConferenceCancelled`. `ConferenceAttendanceDeclined` event + `DeclineConference` command/service/context (folds "still live?" from the event stream) + `DeclineConferenceController` and a Decline action on `/tentative-conferences`; four projectors drop the conference. Uses `archived/ConferenceSubmissionTrackingPlan.md`'s own event name (forward-compatible with that plan's full two-dimension status, still open). New `/tentative-conferences/*/decline` route with its own `SecurityConfig` matcher + `AuthorizationMatrixTest` row. | `c08896a`, 2026-08-16 |
| **Multi-segment flight lookup offers each leg.** AeroDataBox returns one entry per segment for a flight number; the client used to collapse them into leg 1's departure and leg N's arrival, inventing a route no leg flies (UA 1604 → `RDU → RNO`). `FlightLookupCandidates` now carries every segment and reports whether a choice is needed; `book-flight` / `change-flight` render a shared "Which flight?" fragment and post the chosen segment back to `.../lookup/select` (no second API call). The whole trip is offered as an extra option only when the segments chain end to end. Also seeds the lookup date from `?date=` or the booking's own departure-zone day. **New route `/booked-flights/*/lookup/select`** — two segments past the id, so no existing matcher covered it; it has its own `SecurityConfig` line and `AuthorizationMatrixTest` row. | `a2b1845`, 2026-08-14 |
| **Owner home page redesign** — two-column viewing/editing layout for OWNER. The nav-card items in `Cleanup_Tasks.md` (state-aware "Schedule problems" card) still apply to it. | `5277366`, 2026-08-13 |
| **Owner future-day create menu on the calendar** — clicking a future day offers "Add flight / gathering / conference…", seeding each form via `?date=`. | `93a78ad`, 2026-08-12 |
| **Push one entry to Google Calendar** (`web/GoogleCalendarLink`). Ted schedules real work against Google Calendar, so a conference has to *be there* to be scheduled around — "book dinner after the last session" is the requirement, and the subscription feed cannot answer it (Google refreshes a subscribed URL on its own clock, hours, uncontrollable, and fires no notifications from one). **It is a plain link, not the Google API**: `calendar.google.com/calendar/render?action=TEMPLATE` opens Google's editor pre-filled and Ted presses Save — no OAuth, no stored refresh token, no SDK, no scheduler, and nothing the server sends, which keeps the property the `.ics` feed was chosen for (no exactly-once hazard; a replay or restore cannot re-push, because pushing is a thumb). **Wall-clock precision is the whole point**, so `dates` carries local basic-format times with no `Z` and `ctz` names the IANA zone — exactly what `ZonedTimestamp` already holds, so a venue's 09:00 arrives as 09:00 there. Placement, Ted 2026-09-08: **conferences** get icon + "Add to Google" on a line under the Dates cell (dates and calendars are one subject, and it keeps the Actions cell at the three state-machine moves it is capped at — a fourth link there would force a dropdown); **gatherings and private events** get the icon alone to the right of the When value, where the column has horizontal room. Its **own** FA Pro `calendar-arrow-up`, never a borrowed pencil or bin. On every row in every state, so it never moves. **Three things accepted knowingly**: it is fire-and-forget (press twice, Google has two events; nothing records that a push happened, so a later edit or cancellation is fixed by hand — the alternative is OAuth plus a persisted entry-to-event mapping); a multi-day conference arrives as one timed event, which Google files in its all-day banner rather than the time grid, with the exact start and end in the detail (Ted chose this over one-event-per-day); and no on-device probe was run first. **Confirmed working on the laptop, which is where Ted does calendar work** (Ted, 2026-09-08); the iPad is deliberately not a concern for this feature, and the deployed link is to be confirmed in production. OWNER surfaces only: the URL carries the title and venue in the markup, and no calendar entry carries it (`EntryDetails.Publishable` has no slot). Mutation-verified in three rounds; suite green both tiers (2370 + 78). | 2026-09-08 |

## Not a task list

These describe how things are, not what to do next, and belong in no status column:
`JS-Behavior-Tests.md`, `EventSourcingRulesHeuristics.md`, `MigrationLessonsLearned.md`,
`ConferenceStateMachine.md`, `DEPLOYMENT.md`, `lodging-event-model.md`,
`ChangeFlightSlices.md`, `Features.md`, `EventModel.txt`, `EventLanesDoc.txt`,
`Tentatively-Plan-Conference-slice.txt`, `docs/slices/`.
