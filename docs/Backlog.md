# Backlog — index of every plan doc and open item

**This is an index, not a backlog of its own.** Each row points at the doc that owns the detail;
the "What's left" column exists only so you can tell, without opening twenty files, whether
something is still live. When work lands, update the owning doc first and this table second.

Finished plans live in **`archived/`** (see `archived/README.md`) — kept for the reasoning, not
tracked. A plan moves there only once it owns no remaining work.

Small cleanups keep living in `Cleanup_Tasks.md` — they are summarized here but not duplicated.
Decisions made during implementation that still need Ted's eye live in `DecisionsToReview.md` —
a review queue, not a backlog; work through it one entry at a time.

Status verified against the tree on **2026-08-21** (`d4a9a3f`).

Legend: `open` · `partial` · `done` · `exploration` (deliberately unbuilt design record)
· `decision` (waiting on Ted, not on code)

---

## Open features

| Item | Owning doc | What's left |
|---|---|---|
| **Schedule problems rewrite** | `archived/ScheduleProblemsRewritePlan.md` | `done 2026-08-20` — **open: the mirror image of D14** (a gap *into* home still spans every day from the last away fact to the next home departure; collapsing it moves `relevantUntil` earlier, so it needs a decision); `DifferentCityConflict` still excludes private events because the *clearing* event types its subject as a `GatheringId` (detection is indifferent) — **tabled 2026-08-20**, fixing problems easily comes first; the work is an additive `PrivateEventCityConflictCleared` plus a one-of subject. It was pencilled in for problem-calendar slice 4, but **slice 4 shipped 2026-08-20 without it** — clash *markers* only — so it now lives in `Cleanup_Tasks.md` under **Deferred (until needed)**, with its trigger named: Ted plans a private event on a day a conference runs elsewhere. Until then the report under-reports rather than showing a row he cannot clear, which is the safe direction. The Travel-lane question is **closed**: Ted read the live calendar 2026-08-20 and it renders correctly. `/schedule-problems` is wrong in both directions and this replaces its detectors wholesale. The current model is "a list of legs with conferences bolted on": hotels and gatherings never establish position, "adjacent" means adjacent *legs* however far apart, every conference runs its own unbounded nearest-leg search, and `deduplicateMissingTravel` is a symptom suppressor for the garbage those two produce. Missing-hotel nights come only from leg arrivals and conference interiors, so a checkout, a gathering, and a conference's last night all fail to require a bed. Duplicate hotel bookings are not detected at all. Replaced by **one located timeline** — every source contributes a presence fact — read by a left-to-right walk (missing travel, one problem per adjacent pair) and a night sweep (missing hotel, split by city, merged across a conference boundary). Ted's rules: last known location, never "unlocated"; one city namespace for airports/stations/addresses with `locationForMatching` as the only override; no bed needed for a night in transit or at home; duplicate stays are a problem in the same city or different ones. New `ScheduleProblem.DuplicateHotel` variant breaks the exhaustive switches on purpose. Three acceptance itineraries from Ted are the suite; the ~1400-line `ScheduleGapProjectorTest` needs re-judging nest by nest, since two nests exist only to pin the bugs in place. |
| **Problem calendar** | `archived/ProblemCalendarPlan.md` | `done 2026-08-20` — **slice 4 (clash markers) shipped**, the last one: `SchedulingConflict` (red) and `DifferentCityConflict` (violet) now band the calendar, sharing one `Lane.CLASH` under Travel. The colour distinction needed a home that was not a second lane (two lanes would reserve a row per kind on every week), so `ProblemBand` carries a **`Marker`** — `BED`/`DUPLICATE`/`TRAVEL`/`CLASH_CITY`/`CLASH_SCHEDULING`, each naming its lane and deriving its own CSS modifier — with `lane()` kept as a derived accessor, so the packing code was untouched. `ProblemBand.from` **stopped returning `Optional`**: it was empty only for lanes that did not exist yet. A scheduling clash spans min→max of **all four** timestamps (both starts and both ends — a subset reintroduces the inversion the travel band already taught us about) and names both zones; it is deliberately **not an anchor** (F6: no ids on either side), while a city clash opens the same `/clear-conflict` item its list card offers, from the one `ProblemFix` mapping, asserted equal. Wording follows the existing band vocabulary (Ted picked it from three): `City clash — Lunch · dev2next` over `Denver vs Chicago`. Earlier: **slice 5 (fix links) shipped 2026-08-20**. Slice 5: one `ProblemFix.forProblem` exhaustive switch over the sealed `ScheduleProblem` feeds **both** views, so a band and its list card demonstrably link to the same place (a test asserts it). Every card and every band carries one `Fix ▾` control in one place — even for a single answer — because a travel gap has three (flight, train, ground transfer) and a row of links would move the affordance per card, which the "action affordances never move" rule forbids; `SchedulingConflict` shows that control **greyed with its reason** rather than dropping it, since neither side carries an id. The day-menu `<details>` pattern became its second user, so its CSS and dismissal script were lifted out of `CalendarRenderer` into a shared `DisclosureMenu` (classes renamed `.day-menu*` → `.disclosure-menu*`) and `ProblemFixMenuJsTest` exercises the same script from both views. Prefill extends the existing `/book-hotel`, `/book-flight` and `/book-train` GET handlers, so **no new route, matcher or matrix row**; the link carries cities and `BookFlightController` seeds a code only where a city has exactly one (`AirportCityResolver.soleAirportFor`, now one bean with four injection sites). The transfer link carries the **date alone** — the plan's preselection idea was dropped and the doc corrected, because D13 made those ends flight legs and one `airport:` value can belong to several. Original text follows: `in progress` — a second view of the OWNER-only `/schedule-problems` report: the same problems on week rows, so a hole in a trip reads as a hole rather than as three cards. **Slice 1 shipped 2026-08-19** (Bed lane): `/schedule-problems?view=list\|calendar` — **one route**, so the existing `hasRole("OWNER")` matcher covers both and no redaction is in play; a `.view-toggle` List/Calendar selector on both views (styling grouped with `.time-toggle` in `site.css`, but *not* `TimeFilterToggle` — different parameter, different question); `ProblemBand` + `ProblemBand.Lane` + `ProblemCalendarViewBuilder` + `ProblemCalendarRenderer`, all deliberately **separate from `CalendarEntry`/`CalendarViewBuilder`** so the pending S2+E2 refactor keeps one audience. `ProblemBand.from` is an exhaustive switch over the sealed `ScheduleProblem`, so a new problem type must decide where it lands. Lane colours match the list's card columns. `today` from the injected `Clock` + `viewerZone` cookie. **Slice 1.5 shipped 2026-08-19** (context backdrop, built before the Travel lane on Ted's ask): `ScheduleGapProjector` gains a **second read model**, `context()` → sealed `ScheduleContext` (`Conference`/`Gathering`/`Travel`/`Stay`), computed in the same pass from the same state — a second source could disagree with the detector and paint a gap with no cause; `CalendarAggregator` rejected for that plus the `CalendarEntry` coupling. Context renders as a **grey full-height backdrop behind** the problem bands (not a lane of its own — then translucency would mean nothing), bands at **80% alpha**, one grey for every kind with the kind in the label (`dev2next, Chicago · Sep 14–18`), and each context band reserves a label line at the foot of the week so labels stack instead of colliding. Everything in the window, not just the implicated entry — `ScheduleProblem` carries no reference to its cause, so cause-linking waits for the fix-link slice. **Slice 2 shipped 2026-08-19** (Travel lane): `MissingTravel` → amber band spanning **both** ends' local days (min/max, not arrival→departure — those two dates invert when a Tokyo morning precedes a San Francisco evening, and the band would render as nothing), detail line naming **both zones** (`Arrive 2:30 PM BST · depart 9:00 AM CEST`). **Open, both designed in the plan 2026-08-20 and ordered 5 before 4 on Ted's priority ("easily fixing the problems is most important"): slice 5** (fix links on cards *and* bands, from one `ProblemFix.forProblem` exhaustive switch — `/book-hotel?city=&checkIn=&checkOut=`, `/book-flight`+`/book-train?fromCity=&toCity=&date=`, one `/booked-hotels/{id}/cancel` per duplicate stay, the existing `/clear-conflict` URL moved out of the renderer; prefill extends existing GET handlers so no new route, matcher or matrix row; city→airport is many-to-one so the link carries cities and `BookFlightController` fills the code only where a city has exactly one; `SchedulingConflict` gets a greyed "Fix" with a stated reason, since its sides carry no ids; the fixes live behind one uniform `Fix ▾` menu on every card and band — a travel gap has flight, train *and* ground transfer as answers, so the list is open-ended and a row of links would move the affordance per card — reusing the `<details>` day-menu pattern and its dismissal script, second user, plus a js-tier test) and **slice 4** (Clash lane, violet/red inside one lane, `SchedulingConflict` spanning min→max of both sides' local dates, clear-conflict on the band via slice 5's mapping). The third answer to a travel gap is **settled 2026-08-20**: `archived/GroundTransferPlan.md` (own entry kind, own presence fact, so it closes the gap rather than silencing it) — its fix item is `/plan-ground-transfer?date=`, which seeds the endpoint `<select>` lists, preselecting an end only where exactly one option resolves to the gap's city; **ground transfer is the better slice to build first**, after which the menu ships complete. **Default flipped to `calendar` 2026-08-21**, after Ted reviewed the view and had its two real faults fixed — every band amber rather than one blue kind he scanned past, and a visible fix chip instead of a click target you had to know about. Still open: whether a day number links to `/itinerary?date=`. |
| **Problem context on fix pages** | `ProblemContextOnFixPagesPlan.md` | `done 2026-08-21` — a fix link from `/schedule-problems` (either view) or from the itinerary's bedless-day row now lands on a form that says **why**: an amber banner above it carries the problem in the report's own words, the causes from `ScheduleContext` clipped to its days ±1 (max four), and a Back link to the surface he left. Ted was opening a second tab to re-read the report. The link carries a **reference** — `?problem=<key>` derived from the problem's own content (`ProblemRef`, exhaustive switch), never the words: a URL can lie and can go stale, a lookup against `problems(now)` cannot, and no match renders nothing. Wording is **reused**, not rewritten: `ProblemBand` and `ContextBand` already hold it, so the banner cannot disagree with the band that was clicked (the planned `ProblemWording` was dropped). D6 settled as **one `@ControllerAdvice`** (Ted): no controller gains an unused dependency, and `ProblemContextFragmentConventionTest` — which walks every href `ProblemFix` emits — is what keeps the wiring from being invisible. `ProblemFix.forProblem` now takes a `FixOrigin` and appends `?problem=&from=` to every href in one place; three callers, no defaulting overload. No new route, so no `SecurityConfig`/`AuthorizationMatrixTest` change — but the banner prints OWNER data, so all six targets staying OWNER-only is a standing condition. **Follow-up shipped same day**: the banner named only the city while the ground-transfer select offered hotels by name, forcing a mental city match (and failing outright for two stays in one city). `ScheduleContext.Stay` gained `hotelName` — so the calendar's grey context bands name the hotel too, Ted okayed the shared change — and the hotel options gained dates + direction-split prefill (D16 in `archived/GroundTransferPlan.md`; the prefill half is on trial). **Open: none.** Ground-transfer *endpoint preselection* is deliberately separate (the gap knows cities, the form takes endpoint tokens, and a wrong preselect writes an event that deletes the gap — D13 in `archived/GroundTransferPlan.md`); it is a later plan, still unwritten. |
| **Private social event kind** | `archived/PrivateSocialEventPlan.md` | `done 2026-08-13` — built + tested (MVP + itinerary entry). Own `EntryKind.PRIVATE_EVENT`, `PlanPrivateEvent` command / `PrivateEventPlanned` event / context, `PrivateEventCalendarProjector`, and a redacting `PRIVATE_EVENT` branch in `CalendarEntryRedactor`: anonymous viewers see `Busy / 7:00 PM → 10:00 PM EDT / Toronto, Canada` and nothing else — the one redacted output that keeps a zone-labelled time, via the new plain-text `SubtitleLine.FixedRange` (no `<time datetime>` leak). Plan form + controller + nav card (FA Pro icon placeholder pending Ted), and a `PrivateEventItineraryEntry` on `/itinerary`. Both tiers of redaction test (`CalendarEntryRedactorTest` + `CalendarRedactionSecurityTest`, mutation-verified) plus command / projector / controller / golden-sample tests; full suite green (790). **Rendering details resolved:** redacted time is a plain-text zone-labelled `FixedRange`; owner view reuses the shared `EventCalendarSubtitle`. **Deferred to follow-ups (see plan doc):** Change Private Event (edit flow) and a `/planned-private-events` list view with the FUTURE/ALL toggle. |
| **Event-oriented backup/restore** | `archived/EventOrientedBackupRestorePlan.md` | `done 2026-08-11` — replaced command-replay export/import with event-**verbatim** backup/restore (commands kept as opaque history for future undo). Restore reuses stored event ids/sequences/timestamps; read models rebuild on **restart** via the boot replay — the live `reset()`/`rebuildFromPersistence()` rebuild was built then **reverted** (email-sender hazard), so the stale-after-truncate bug is unchanged (a restart clears it). **Backup-format break** (v2; old command-only files not read). Unblocked `DecisionContextQueryDesign.md` and cleared the private-social-event sequencing blocker. |
| **Conference submission tracking** | `ConferenceSubmissionTrackingPlan.md` | `in progress` — designed 2026-08-12; **slices 1–2 shipped (`ConferenceFormat` 2026-08-18, commitment 2026-08-19); slices 3–5 open**. Splits today's single "tentative" into **two dimensions**: attendance commitment (`WATCHING` / `GOING` / `NOT_GOING`) and a per-submission speaking pipeline (CFP pending → submitted → accepted / waitlisted / rejected / withdrawn, plus invited), because one conference takes several talk proposals. Status is **derived** in the projector, so new labels need no new events — most usefully rejected-but-undecided, which becomes a visible "decide". **Redaction (Ted, 2026-08-12): commitment is public, status is private.** Speculative conferences *do* render for anonymous viewers (that is what invites "I'll see you there"), but every speculative state collapses to one public "tentative" label, and talk titles, decisions, CFP dates, commitment *basis*, and all free-text reasons are OWNER-only and must never enter `CalendarEntry`. Adding `CalendarEntry.commitment` deliberately breaks the redactor's conference branch. Also pulls in `datesConfirmed` (held slots carry last year's guessed dates) and attendance filtering in `ScheduleGapProjector`. Cheap now that command-replay import is gone. **Decided 2026-08-12:** `SubmissionAccepted` **auto-commits** attendance (last decision wins, so a later decline overrides — submitting is opting in); `SpeakingInvitationReceived` does **not** (unsolicited offer, needs an explicit yes); a dropped conference leaves the calendar and itinerary entirely but stays on the OWNER-only `/conferences` list, hidden behind a **separate** `?dropped=` toggle orthogonal to the existing FUTURE/ALL one; **acceptance is public** (Ted announces it immediately), so there is no embargo state and no timing rule — but the commitment *basis* still stays OWNER-only, because publishing it makes its absence a free inference about submissions, and no public speaking marker exists anywhere in the app today (gatherings' `speaking` badge is owner/family-only). **No open questions remain.** **First piece shipped 2026-08-16:** the `ConferenceAttendanceDeclined` event + command/handler/projectors + a Decline action on `/conferences` (thin slice, uses this plan's own event name; the full two-dimension status is still open). **Refined 2026-08-18 for CFP season, and slice 1 shipped the same day:** open-space is **not** a new `EntryKind` but a **`ConferenceFormat { CALL_FOR_PAPERS, ACCEPTANCE_REQUIRED, OPEN_SPACE }`** field on `ConferencePlanned` — **SHIPPED 2026-08-18** (non-null field, radio-button picker defaulting to CALL_FOR_PAPERS; schema bumped to **v3** with the upcaster injecting absent format→CALL_FOR_PAPERS as an independent increment; `datesConfirmed` **deferred** to the slice-5 Schengen ceiling that consumes it). (PLoP is `ACCEPTANCE_REQUIRED` — rejection auto-drops, vs `CALL_FOR_PAPERS` "decide: go anyway"; `OPEN_SPACE` rejects any `CfpOpened`); events renamed to "what happened" (**`CfpOpened(closesOn)`**, `TalkSubmitted/Accepted/Rejected/Waitlisted/Withdrawn`, `InvitedToSpeak`, `ConferenceAttendanceConfirmed`); submissions are **conference-keyed, no per-talk `SubmissionId`/title yet** (fold = best-outcome-wins); **backfill RESOLVED** = one-off pass through the real UI (record end-state, never reconstruct); CFP closing deadline rides the **existing owner-only iCal feed** as a VEVENT with **72h + 24h VALARMs** (this is the 2nd feed contributor, so `ICalEventSource` is introduced here); **build order re-sequenced for CFP season** (**format [DONE 2026-08-18]** → commitment fold+backfill → `CfpOpened`+CFP-deadline reminder+radar → submission stream+badge → Schengen/gap+`datesConfirmed`). **Slice 2 SHIPPED 2026-08-19:** `AttendanceBasis` is **three values** — `ATTENDING_ANYWAY` dropped, because buying a ticket *is* how "go anyway" happens and the after-a-rejection narrative is already carried by the preceding `TalkRejected`; the public calendar gets a **"Maybe" chip on speculative conferences only** (a `Going` chip would be noise on every committed one, and its absence would become inferrable), **identical for owner and anonymous**, with the collapse done **in the projector** so `basis` and submission status never enter `CalendarEntry` at all; richer per-conference status waits for the slice-3 radar. **As built:** `ConferenceAttendanceConfirmed(conferenceId, basis, confirmedOn)` (basis non-null, fails loud) + `ConfirmConferenceAttendanceCommand`/`Context` + a `ConfirmConferenceAttendance` service folding "still live?" from the stream exactly like `DeclineConference` — refusing only a conference that is gone, and **allowing a second confirmation with a different basis** (ticket bought, then talk accepted; last one wins). The derived `AttendanceCommitment` (in `application`) has **two** values, `WATCHING`/`GOING` — no `NOT_GOING`, since declined/cancelled conferences leave every read model, so "not going" is absence. Both conference projectors fold it: the calendar one stamps `CalendarEntry.commitment` → the `.entry-maybe-badge` chip (non-continuation segments only), the list one stamps `ConferenceView.commitment` → a **Going?** column on `/conferences` with a **Confirm** link shown on `WATCHING` rows only (Decline stays on both). New `/conferences/*/confirm` route (`SecurityConfig` + `AuthorizationMatrixTest` row) with a dedicated Thymeleaf page and three radio buttons; a missing/unknown basis re-renders **that** page with the error rather than redirecting to the flash-less list. Both tiers of redaction test, all mutation-verified; `CLAUDE.md` amended (the private list gained the submission pipeline / CFP dates / `AttendanceBasis`; "Public by decision" gained the chip **and its collapse precondition**). **`TalkAccepted` auto-commit and `InvitedToSpeak` offer-to-commit moved to slice 4**, with the events they fold. **Decided 2026-08-19 (planned, not built):** the OWNER-only list also gains a **`SPEAKER`** marker in the `Going?` column — that word, not the calendar's longer "A Ted Talk", because the column is a nowrap unit in a seven-column table that only just fits; sourced from `AttendanceBasis` until slice 4 and from the submission fold after, carried as a derived `speaking` boolean on `ConferenceView` (never the basis itself); lands with slice 3's radar. **Sequencing decided:** slice 2 shipped *before* the S2+E2 calendar refactor, so `commitment` is for now an 11th, nullable field on the flat `CalendarEntry`. **Backfill is now unblocked and is Ted's to run** (dev2next / ExploreDDD / SoCraTes through the real UI; J-Fall still needs slice 3's `CfpOpened`). **Rename landed first (2026-08-19, separate commit):** `TentativeConferenceProjector`/`View`/`TentativeConferencesRenderer` → `ConferenceProjector`/`ConferenceView`/`ConferencesRenderer`, `PlanTentativeConference{Command,Handler,Context,Request}` → `PlanConference*`, and the route **`/tentative-conferences` → `/conferences`** — because once commitment is its own dimension, "tentative" is the concept being removed and a `GOING` conference in a class called `Tentative*` is an active lie. **The event was renamed too, same day, on a second look:** `ConferenceTentativelyPlanned` → **`ConferencePlanned`**, class *and* `EventTypes` logical name — because the first rename left `PlanConference` emitting the only event in the model that does not share its command's stem, and "tentatively" asserts a commitment status this plan makes *derived*. `EventTypes` aliases **both** retired wire ids (the old logical name and the pre-logical-name FQCN) so stored rows and older backups keep resolving; both upcaster rungs moved to the new logical name; the golden legacy sample deliberately keeps the old one. `event_log` therefore holds both spellings until the normalization pass is run — that pass is **built** (2026-08-19), see the `event_log.type` row below. The unrelated hotel-status `Tentative*` family is unchanged. Related: the `ConferenceCancelled` and `infoUrl` rows below. |
| **Calendar subscription feed (cancel-deadline reminders)** | `archived/CalendarSubscriptionFeedPlan.md` | `done 2026-08-18` — Phase 1 implemented, both tiers green (927 unit + 36 js), deployed to prod (Railway `CALENDAR_FEED_TOKEN` + `JITTERTRAVEL_BASE_URL` set), and the **on-device Critical Validation Gate PASSED**: an alarm fired from the real `webcal://` subscription on Ted's iPhone; Ted is on the live feed with the weekly heartbeat as the standing liveness signal. **Key gotcha found & documented:** iOS defaults **"Remove Alerts" ON** for a subscribed calendar and hides the toggle at subscribe time — alarms are silently suppressed until you open the *subscribed calendar's details* and turn it OFF (per subscription, real feed included); the admin card's gate instructions were rewritten to point there. A private, **token-gated** iCalendar (`.ics`) feed Ted subscribes to on iPhone/iPad (built-in Calendar, no app); each hotel booking with a `cancelBy` becomes a VEVENT with **three `VALARM`s — 48h, 24h and 4h before** the deadline (the 4h alarm also covers a booking made <24h out) — which **iOS fires locally**, so it works offline/roaming, unlike SMS. Chosen over SMS/email/push because the feed is a **pure projection**: no server scheduler, no 3rd-party provider, and **no exactly-once/replay hazard** (nothing is "sent"). Reuses `BookedHotelView.cancelBy` (no new projector). **Security is the crux:** the feed is **unredacted OWNER data**, so the URL token is the only credential — configured secret (`CALENDAR_FEED_TOKEN`, absent ⇒ 404), constant-time compare, wrong/missing ⇒ 404 empty body, `/calendar/feed/**` matcher + `AuthorizationMatrixTest` row + a dedicated `CalendarFeedSecurityTest`; the subscribe link lives on an OWNER-only `admin-home` card. **Built (all round-1 + round-2 decisions resolved):** three alarms (48h + 24h + 4h, multiple VALARMs per VEVENT); short timed event at the deadline instant; single shared env token; a **weekly liveness heartbeat** VEVENT (early warning if the pipeline goes silent); and a **dual-mode probe endpoint** `/calendar/feed/{token}/probe.ics` (a synthetic event ~10 min out with a ~5-min alarm) consumed two ways — `https` + "Add All" (owned-event path) and `webcal://` subscribe (real subscription path) — so the on-device alarm path is testable in minutes with **no hotel booking**. The OWNER-only `admin-calendar-feed` card surfaces the subscribe + both probe links and the "keep Remove Alarms OFF" gate instruction; reached from an `admin-home` nav card. **Phase 1 = cancel deadlines only**; the assembler returns a `List<ICalEvent>` so a **full travel calendar** (flights/trains/hotels/gatherings/conferences) can be layered on later — no `ICalEventSource` interface until the 2nd contributor (no abstraction before 2nd user). **Only remaining work is the manual on-device validation gate**, which cannot be automated. |
| **Schengen 90/180 day counter** | `SchengenDayCounterPlan.md` | `open` — planned 2026-08-12, **amended twice on 2026-08-12** (second amendment verified against the 2026-08-11 backup), nothing built. **Design now an evidence hierarchy, not one union:** tier 1 = **external Schengen border crossings** → entry/exit envelopes, authoritative, every interior day counts; tier 2 = union of conference/gathering/hotel dates, fallback for days no envelope covers; tier 3 = Ted's explicit assumed stays. **Prefer legs over hotels (Ted, 2026-08-12)** — hotels empirically carry the past on their own, but they evidence where he slept, not passport control; dropping them is not viable because the pre-submit warning exists for trips with no travel booked. Crossings come from **trains as well as flights** (the June trip's exit is the Brussels→London train; flights-only pairing over-counts by 4 days). Consequences: airport **membership** (a boolean, not full airport→country) moves from last to step 1; **no historical backfill is needed** (conferences contribute 0 unique past Schengen days — measured); candidate gaps clamp to the next known presence *anywhere*; an **open envelope** (entry with no exit) needs its own candidate type. Real numbers on current data: 20/90 used today, peak 45 on 2026-11-19; booking one FRA→SFO exit moved 9 days from assumed to confirmed. **Layovers (Ted, 2026-08-12):** always entered as separate flights, so a Lisbon stopover en route to Morocco produces a same-day Schengen envelope — the "invisible layover" limitation is void and replaced by a **transit candidate** where the default is counted and Ted's click removes a day. Rest of the row unchanged: Two surfaces: an owner-only current+peak strip on `/calendar`, and a pre-submit "days remaining" panel on the planning forms that fires on `country` blur (to decide Schengen membership) and `endDate` blur (to recompute). Presence is a **union of dated country intervals** from conferences, gatherings, hotels and trains — not a chain of legs — so a conference booked before any flight still counts, which is the normal order of work. **No padding** (Ted, 2026-08-12): the count rises as flights and hotels are added, and that is correct. **Amendment (Ted, 2026-08-12): two numbers, not one** — a confirmed floor (committed conferences + booked travel) and a worst-case ceiling (+ live speculative conferences + explicit *assumed stays*), from the same method called twice. Assumed stays are how contiguous-ish conferences get bridged when Ted stays in the region instead of flying home; **per-gap, no inferred tolerance** (Ted's call) — the app lists candidate gaps, Ted marks them, and the assumption is an event, i.e. just another dated interval into the union. Warning fires when **either** number hits 85, which in practice means the ceiling. **Depends on** the commitment events from `ConferenceSubmissionTrackingPlan.md` — without them every conference is speculative and the floor is always zero. Only missing datum is airport→country (`StaticAirportCityResolver` is city-only), sequenced last, and it also gates the "booked flight home breaks the gap" rule. Needs `SecurityConfig` matchers + `AuthorizationMatrixTest` rows for `/api/schengen-preview` and the assume/withdraw POSTs, and a `CalendarRedactionSecurityTest` case for the strip. |
| **Booking provenance — how a booking was made** | `BookingProvenancePlan.md` | `open` — requirement captured 2026-08-19 (Ted), **nothing designed, nothing built**. Every booked hotel, flight and train should record the **channel** it came through — Booking.com vs the hotel direct, Expedia vs the airline, a reseller such as railfinder vs Deutsche Bahn — because that decides **where the ticket is** and **how (and by whom) it can be cancelled or changed**: a Booking.com reservation is not cancelled by phoning the hotel, and an airline will often decline to touch an agency-issued ticket. Pairs with the existing `HotelBooked.cancelBy` deadline and its iCal alarms (`archived/CalendarSubscriptionFeedPlan.md`) — the alarm says *when*, provenance says *where and how* — so the **cancel-hotel page** is the surface where it earns its keep. **All design questions open:** shape (a closed enum will not hold an open-ended provider list; likely channel + provider name, possibly with booking reference and a manage-booking URL), whether it lands as new fields on `HotelBooked`/`FlightBooked`/`TrainBooked` + `*Changed` (a schema bump and upcaster increment per type, like `ConferenceFormat`) or as its own after-the-fact event (friendlier to backfill), backfill via the existing edit forms, and which surfaces show it. **Redaction is settled, not open:** OWNER-only, and it must never enter `CalendarEntry` — CLAUDE.md already classes booking references and booking URLs as private, so keep it out of the calendar read model entirely rather than stripping it in the redactor (the `AttendanceBasis` pattern). |
| **Post-deploy task banner** | `PostDeployTaskBannerPlan.md` | `partial` — **slice 1 shipped 2026-08-19** (registry + `OneOffTaskCompleted` + OWNER-only home banner + `/admin/tasks`, two real tasks declared; suite green 1046 unit + 36 js). **Both declared tasks were run in production 2026-08-21 and retired the same day** — the conference-attendance backfill (`13:03:29Z`) and the `event_log.type` normalization (`15:27:49Z`) — so `OneOffTaskRegistry` is now **empty**, which is a normal resting state and not a gap: the banner hides itself and `/admin/tasks` reads "Nothing declared." Each `OneOffTaskCompleted` event stays as history and nothing refers to the ids. That exercised the whole latch end to end, declaration through retirement. Steps 2–3 open: derivable checks (legacy-row count, missing config) and the `declaredOn` age check. Designed 2026-08-19. A one-way latch for **one-off tasks that follow a deploy**: migrations to run and backfills of data that did not exist before (scope narrowed by Ted — a hotel with no `cancelBy` is deliberately NOT a task, and a `CALL_FOR_PAPERS` conference with no `CfpOpened` is at most a low-priority timeout). Tasks are **declared in code** and only the completion is an event (`OneOffTaskCompleted`, appended via `CommandExecutor.appendEvents`) — Ted's original sketch latched the declaration in with a **startup append**, which the doc recommends against: it would throw in read-only mode (exactly when the banner is most needed), needs per-boot idempotency, writes deploy artifacts into the backup stream, and would outlive the code that explains it. Splits **derivable** tasks (count legacy rows, missing config — self-clearing) from **acknowledged** ones (a backfill cannot be detected: `WATCHING` with no confirmation is indistinguishable from genuinely undecided). Renders as an **OWNER-only** banner at the top of the home page, same slot/shape as the read-only banner but gated like `showDataEntryNav`. The "don't leave inert code lying around" loop is **advisory, never blocking** — a pre-push hook would need a token-gated endpoint and fails offline, so the doc recommends starting with a `declaredOn` age check in the test suite, with the weekly iCal heartbeat as the other no-network option. **First real customer already exists:** the `event_log.type` normalization from `5df5358` is outstanding against prod. **All five open questions answered by Ted 2026-08-19:** ticking off lives on a separate `/admin/tasks` page (banner links to it, so the route needs a matcher + matrix row); a completed-but-still-declared task shows **only there**, greyed "code can be removed", never in the banner, which disappears entirely once nothing is outstanding; **missing config is a task in the same list** (derivable, self-clearing, covers the only silent failure mode); **restart-after-restore is out of scope** and belongs with the read-only banner instead, since it means the data on screen is stale *now*; and the cleanup nudge is the `declaredOn` age check alone — the pre-push hook and its token-gated endpoint stay unbuilt. |
| **Public "speaking" badge on the calendar** | `ConferenceSubmissionTrackingPlan.md` | `partial` — **gatherings half shipped 2026-08-17**; conferences half still open. **That Ted is speaking at a conference or gathering is never private** — the event's place and time are already public — so it renders on the **anonymous** `/calendar`, not just owner surfaces. Supersedes the earlier draft that withheld it to avoid an absence-inference; "attending without speaking" is ordinary and reveals a rejection only to someone who already knows he submitted, which stays private. **Two halves, different readiness.** *Gatherings: **done 2026-08-17*** — threaded `GatheringPlanned/Changed.speaking` through `GatheringCalendarProjector.toEntry`, added a `boolean speaking` field to `CalendarEntry` (before `editPath`; both convenience constructors default it `false`, so non-speaking call sites are untouched), kept it on the redactor's GATHERING branch (now split from CONFERENCE, which drops it) with every redactor branch rewritten to the full canonical constructor so a future field still breaks compilation there, and render a solid high-contrast `.entry-speaking-badge` chip reading "A Ted Talk" in `CalendarViewBuilder.renderEntrySegment` (non-continuation only; the itinerary's own "Speaking" badge is unchanged). Both tiers of redaction test (`CalendarEntryRedactorTest.gatheringSpeakingFlagSurvivesRedaction` + `CalendarRedactionSecurityTest.anonymousUserSeesSpeakingBadgeOnPublicGathering`) plus projector + renderer tests, all mutation-verified; `CLAUDE.md` redaction section amended. *Conferences: waits for step 4 of the owning plan*, since the speaking fact only exists once submissions do. **Caveat (Ted, 2026-08-12): a private talk at a company is NOT this** — no public venue or time, so it must get its own `EntryKind` rather than be modelled as a conference or gathering to earn a badge; same trap as the private dinner, sibling of `archived/PrivateSocialEventPlan.md`. |
| **Replace Hotel** | `HotelCancelReplacePlan.md` | `partial` — Phases 0, 1 (`cancelBy`) and 2 (Cancel Hotel + deadline column) are done. Phase 3 (Replace: cancel the old booking and book a new one linked by `replacesHotelBookingId`) is unbuilt and needs a second `HotelBooked` schema bump. **Owning doc is kept as history and now carries a superseded banner (2026-08-13):** the check-in gate it specifies throughout was removed (`CancelHotelContext` is `bookingExists` alone; `CannotCancelAfterCheckIn` deleted), and cancel no longer hard-removes from `/booked-hotels`. Phase 3 is left as written but is partly invalidated — its `ReplaceHotelContext` (old `checkIn` + `now` "for the cancel gate") must be re-derived before building, and the replaced booking now *is* renderable on `/booked-hotels`. Rationale settled in `DecisionsToReview.md` S3/S5. |
| **Time-ordered calendar days** | `Future_Feature_Slices.md` | `open` — deferred by Ted 2026-08-20, on seeing a 3:55 PM flight render *above* the 1:00 PM ground transfer that fed it. **The lanes stay as they are**; this records what changing it costs. The calendar is not time-ordered within a day and cannot be without abandoning its layout model: each week is a grid of fixed per-kind **lane bands** in `EntryKind` order, so an entry sits in its kind's band regardless of when it happens (the same reason a 9 PM gathering renders above a 7 AM flight). **Reordering `EntryKind` does not fix it** — a ground transfer runs both ways, so above-FLIGHT reads right for the taxi *to* the airport and backwards for the taxi *from* it; the question is about time, not kind. The **itinerary already sorts across kinds by `anchorTime`** (pinned by `aTransferToTheAirportSortsAboveTheLaterFlightItFeeds`), so the time-ordered view exists — the calendar just cannot join in. Hard part is **multi-day entries**, which are why lanes exist: a week-spanning conference or stay has no single row it can occupy without colliding with each day's timed entries, so it likely needs its own band anyway — lanes split by *duration* rather than by kind. Most of `CalendarViewBuilder.renderWeek` (`byKind`, sub-row allocation, `kindOffset`) goes, and `CalendarViewBuilderTest`'s stacking coverage gets rewritten rather than extended; kind colours, CSS classes and continuation arrows are unaffected. Natural companion to the `EntryDetails` refactor in `RendererVsProjectorResponsibilities.md`, which already touches `CalendarEntry` and the builder. |
| **Undo Cancel Hotel Booking** | `Future_Feature_Slices.md` | `open` — added 2026-08-13, nothing built. Reinstates a cancelled stay under its original `HotelBookingId` instead of forcing a re-entry. **Direct consequence of removing the check-in gate on Cancel Hotel** (same day): with no gate, the thing that keeps a mistaken cancel harmless is a cheap reversal, and today there isn't one. The greyed-out "Canceled" row now on `/booked-hotels` is deliberately action-free because Undo is the action that belongs there. Has to reinstate into *every* read model that drops the booking (calendar, itinerary, schedule problems, both tentative-hotel projectors, hotel details) — mirror each case in `HotelCancellationPropagationTest`. Shares its reinstate machinery with Phase 3 of `HotelCancelReplacePlan.md`. |
| **`ConferenceCancelled`** | `Future_Feature_Slices.md` | `open` — also the prerequisite for any slice that retracts a booking. Gathering cancellation is the same gap (explicitly out of scope in `archived/ChangeGatheringPlan.md`). The event record and the projector branches already exist (`MigrateConferenceToGathering` emits it); what is missing is an owner-facing cancel action. Note it means *organizers cancelled* — `ConferenceSubmissionTrackingPlan.md` adds `ConferenceAttendanceDeclined` for "Ted decided not to go", which is a different fact — **that decline slice shipped 2026-08-16** (event + command + projectors + a Decline action on `/conferences`); the organizer-facing *cancel* action described in this row is still open. |
| **`infoUrl` on conferences** | `Future_Feature_Slices.md` | `open` — gatherings have one; conferences don't. `ConferenceSubmissionTrackingPlan.md` treats conference `infoUrl` as **public**, so it can ship with that work. |
| **`mapsUrl` on conferences** | `Future_Feature_Slices.md` | `open` — auto-computed from venue + address, as hotels do. |
| **Ground transfer entry kind** | `archived/GroundTransferPlan.md` | `done 2026-08-20` — built + tested. The taxi/subway/shuttle hop no entry kind could record: no service id, no booking, catch the next one. Raised by a real case — a conference in Lone Tree, CO with the flight into DEN — where the journey is real but was unrecordable, so `/schedule-problems` reported missing travel with nothing Ted could enter to answer it. (The *other* half of that case, a conference having no `locationForMatching`, is still an open `Cleanup_Tasks.md` fix.) Shipped as designed, D1–D12 unchanged: `GroundTransferPlanned` + `PlanGroundTransfer` command with an **empty** `PlanGroundTransferContext` (D6 — any date, past or same-day, so the only rule is arrival-after-departure); own `EntryKind.GROUND_TRANSFER` between TRAIN and LODGING with its own taxi-yellow lane rule and its own redacting branch (anonymous sees `Ground transfer` + `DEN → Lone Tree, CO, US`, never the hotel name and **never a time** — rule 2, asserted on the raw markup so a leaked `datetime` attribute fails); a `ScheduleGapProjector` `Movement` case that **closes** the gap rather than silencing it (acceptance test asserts the Denver→Lone Tree gap present without a transfer and absent with one); no addresses typed — each end is a `<select>` token (`airport:DEN` / `hotel:<bookingId>`) resolved server-side and **snapshotted** into the event, with both option lists FUTURE-scoped (D10); day-menu item (D9) and a `/plan-ground-transfer` matcher + matrix row. **D13 added the same day, after Ted used the form** (*"otherwise I have to remember that information or open up another browser tab"*): the airport end is now a **flight leg**, split by direction — "From" lists only arrivals, "To" only departures, since you never travel away from an airport you are departing — and choosing one prefills the date plus that end's time (`DEN — Denver · arrive Mon Sep 14, 11:30 AM (UA 59)`), nudging the far end by 45 min only when the pair would otherwise invert. This **reverses the plan's "no JavaScript" note**: prefill on `change` has no server-side equivalent that isn't a round-trip, and `GroundTransferPrefillJsTest` lifts the script verbatim out of the shipped template so there is no second copy to drift. The **token is unchanged** (`airport:<CODE>`) — a transfer is between places, not flights, so command/event/resolver and D3/D12 are untouched and the times ride on the `<option>` as `data-date`/`data-time`. Leg scoping **sharpens D10**: each leg is scoped by its own moment, so a flight in the air still offers the airport it is about to land at. **D14 then widened D10 outright** — the lists run from **today** onward rather than from *now*, judged in each endpoint's own zone, because scoping to the minute lost today's own endpoints part-way through today (you land at 11:30 and write the taxi up that evening, by which point the airport had vanished). Applied to **hotels as well as flight legs** — not asked for, but the same hole in the most common transfer there is: check out at 11:00, ride to the airport, and the hotel had already gone. Yesterday still drops off. **D15** then cut the owner view back to what Ted actually reads: the calendar entry is title + times and the itinerary card is both ends + times, because the first cut stated the same hop twice (`DEN → Denver Marriott South at Park Meadows` above `DEN → Lone Tree, Colorado, United States`). The publishable route moved to a new `CalendarEntry.publicRoute` field that **no renderer reads** — kind-specific like `commitment`, another passenger for the `EntryDetails` refactor — and adding it forced the redactor's other five branches to declare it, which is deny-by-default working. Ordering: the **itinerary** sorts across kinds by time (pinned by a test — a transfer to the airport sits above the flight it feeds); the **calendar** cannot, since it lays entries out in fixed per-kind lanes, so a transfer always sits in the same band relative to flights whichever way it runs. Four implementation shapes are recorded in the plan's **As built** section (one endpoint-resolver collaborator rather than four lookups on the handler; each end resolves its own zone so a stale *destination* is caught; the publishable route line rides first in the owner's subtitle because the redactor cannot derive a city from a hotel name). **Cancel shipped 2026-08-20**, the D11 fast-follow, the day after: `GroundTransferCancelled` (the id alone — no reason, since a transfer has no booking to explain away), a command refusing only an unknown or already-cancelled transfer with existence folded from the event stream (R1, as `CancelHotel` does) and **no time gate**, since a past hop that never happened is the entry most worth removing; `/ground-transfers/{id}/cancel` GET-confirm + POST with its own matcher and matrix row, landing back on `/itinerary?date=` the transfer's own day; hard removal in the calendar, itinerary, new details-view and **gap** projectors, the last pinned by `GroundTransferCancellationPropagationTest` asserting the Denver→Lone Tree gap *returns* — the masking failure is the whole point. Amber, plain confirm, no typed word (re-entering it puts it back). Reachable from **both** surfaces on Ted's call: a `.cancel-bin` in the edit pencil's slot on the itinerary card and the calendar entry, which needed a new `CalendarEntry.cancelPath` — a sibling of `editPath`, the third kind-specific passenger for the `EntryDetails` refactor, whose addition made all seven redactor branches declare it. Anonymous and family viewers get **nothing**, not a greyed control: the href would disclose both the surface and the id, and the two barriers (redactor nulls it, renderer gates on `isOwner`) each have their own test, with the security-chain case going red only when both break. **Still open, by decision:** no *change* flow — correcting is cancel-then-enter, and nothing is lost in the round trip since both ends are snapshots by design; tracked in `Cleanup_Tasks.md` under **Deferred (until needed)**, triggered by Ted re-entering transfers often enough to notice, most likely once a mode/notes field arrives (D7); no venue endpoint (D12); no `mode` enum (D7); no list page; no zone- or midnight-crossing transfer. Unblocks `archived/ProblemCalendarPlan.md` slice 5, whose `Fix ▾` menu was waiting on this third answer to a travel gap. |
| **Normalize the `event_log.type` column** | `archived/EventTypeColumnNormalizationPlan.md` | `done 2026-08-21` — built 2026-08-19 and **run in production 2026-08-21** (`15:27:49Z`), so the column now holds one spelling per type; the task declaration was retired the same day. The aliases in `EventTypes` **stay** — append-only, and a pre-normalization backup still restores the old names. Two post-click runbook steps are unconfirmed and tracked in `Cleanup_Tasks.md`: take a fresh backup (the new clean floor) and re-check that the migration page reads "Nothing to migrate". The `ConferenceTentativelyPlanned` → `ConferencePlanned` rename left three wire ids for one type, all aliased in `EventTypes`. Rewriting the retired ones to the current logical name rides the **existing** eager migration: a `type` field on `MigratedEventRow`, `type = :type` in its `UPDATE`, a `typeChanged` condition in `LegacyEventMigration.plan()`, and a `renamed` counter plus a one-way-door warning on `/admin/migrate-legacy-events` — all-or-nothing and idempotent like the pass it joins, and it normalizes legacy FQCN rows too. **Counting was the one design correction:** the counters now *overlap* (one row can be rewritten, stamped and renamed in a single write) and the report carries an explicit `toMigrate` row count, because a rename-only row — the common case, since those rows were already stamped — incremented neither old counter and would have left the button reading "Nothing to migrate". **The deciding factor is rollback, not effort:** `EventTypes` aliases are **forward-compatible only**, so normalizing makes the data new-build-only; a rolled-back build fails its boot replay and lands in **read-only mode with empty read models** — no data loss, but a blank site that reads like one. Mitigation is one line: **take and keep a backup immediately before migrating** — it restores into either build. Follow the seven-step **Runbook** in the plan doc. Display-only normalization was rejected: it makes the one screen whose job is to show what is in the database show something else. |
| **Calendar away band** | `archived/CalendarAwayBandPlan.md` | `done 2026-08-21` (`d4a9a3f`) — planned and reviewed against the code 2026-08-20 (`3fca9b6`), built straight through and committed the next morning. A turquoise 4px stripe along the bottom of `/calendar`'s day-label cells for every day Ted is out of town — bottom border only, **no end caps** (dropped 2026-08-20: side borders cost real edge trouble, incl. a same-specificity fight with the amber month-start left border, for boundary marking the ribbon's own ends already do); the label row survives week-collapse, so past trips stay visible even in collapsed weeks. Derived from the **existing location timeline** (`ScheduleTimeline.locationByNight` + `HomeCities`), not from flights — a night is away when its city isn't home *or* it is spent in transit, which is what makes an overnight outbound leg count its **departure day** as the first away day (Ted's explicit rule). Exposed as a third cached `ScheduleGapProjector` read model, `Set<LocalDate> awayDays()` — **a set of days, not trips** (simplified 2026-08-20: with no caps nothing reads a trip's boundaries, so no `AwaySpan`, no run-merging, no interval maths; the whole derivation is ~12 lines in `ScheduleTimeline` and the view builder does `contains(date)`). **Public for all viewers by decision** (aggregates only already-public facts; `CalendarEntry` and the redactor untouched) — needs the one-line CLAUDE.md "Public by decision" addition. **Definition solidified (Ted, 2026-08-20): "away from home" means sleeping away from home** — a trip exists exactly when a night is spent in a hotel, elsewhere, or in transit, and band days are the days bordering those nights; a same-day round trip therefore gets no band *by definition*, not as a limitation. Other guards decided: empty `HomeCities` yields no spans (else everything stripes), `TRIP_BREAK_NIGHTS` inherited. **Code review 2026-08-20 settled four more** (all in the plan): the band is commitment-blind (a `WATCHING` conference or tentative booking bands like any other); a midnight-crossing leg counts as transit even home-to-home, accepting the odd two-day stripe rather than forking the rule; a trip with no return booked ends on its last away night's day (the `+1` return day is earned, not automatic); and adding `ScheduleGapProjector` to `CalendarController` forces mocks into three existing calendar test classes. **Two corrections, both to the trailing-day test:** `locationByNight.containsKey(dn+1)` would have dropped the return day from every trip that *ends* the schedule (the walk fills nights only between points, and a trip's last fact is usually the flight home); and reading the points for a *home* city then missed the mirror case Ted hit on 2026-08-21 — two conferences with no travel booked banded a day short of the second one's closing afternoon, since nothing places him home there. Shipped test is `accountedForOn(dn+1)`: any fact that day, home or away, only ever applied to the day after an away night (so a day trip stays unbanded). Rejected: red-adjacent colours (clash with the amber month border and the red-is-irreversible vocabulary), whole-column tinting, a dedicated ribbon lane, marking home days. |
| **Sunday/Monday week start** | `Sunday-Monday-Week-Start-Switch.md` | `open` — not started. `CalendarViewBuilder` still hardcodes Sunday (`CalendarViewBuilder.java:58`, `:59`, `:64`). Contained to the `web` package. |
| **Viewer-timezone selection** | `Future_Feature_Slices.md` | `partial` — re-read 2026-08-16 against current behavior. Two of the three pieces this asks for now exist: entry times localize to the browser zone (`BrowserZoneScript`, `UtcDatetimeStoragePlan.md` phase 4) with a `ZoneToggle`, **and** the calendar/itinerary notion of *today* is now viewer-zoned via a `viewerZone` cookie captured at login (`0435623`, decision 10 of `UtcDatetimeStoragePlan.md` — see the Shipped table). **Still unbuilt:** the explicit **per-view zone-override dropdown** this doc describes (choose an arbitrary zone on the Calendar/Itinerary, defaulting to the browser's). What exists is a two-state entry-zone/browser-zone toggle, not a free zone picker. Only the dropdown is left. |

## Open cleanups

Detail lives in `Cleanup_Tasks.md`; this is the roll-call.

| Item | What's left |
|---|---|
| Startup-failure (read-only) warning banner | `done 2026-08-16` — home page shows a prominent red `role="alert"` banner to every viewer when `EventStore.isReadOnly()` (failed boot replay or failed save); `GeneralController` injects `EventStore` and exposes a `readOnly` flag. Two mutation-verified `@WebMvcTest` cases; `EventStore` mock added to the two `GeneralController` auth slices. Detail in `Cleanup_Tasks.md`. The companion **boot-replay preflight** row below is the *prevention* to this banner's *detection*. |
| `/booked-hotels` ignores `bookingIntent` | `done 2026-08-15` (`6df5f63`) — `BookedHotelsProjector` now threads the real intent from `HotelBooked`/`HotelChanged` into the view; FINAL bookings read FINAL. Promoted to `EventSourcingRulesHeuristics.md` R8. |
| `ClearConflictController` POST error handling | `done 2026-08-15` (`6df5f63`) — malformed id / generic failure re-render the form via `bindingResult`; `ReadOnlyModeException` redirects to `/read-only`; summary rides as hidden inputs. Three mutation-verified `@WebMvcTest` cases. |
| Eager-migrate legacy bare-scalar events + schema-version stamp | `done 2026-08-16` — owning doc `archived/LegacyEventEagerMigrationPlan.md` (now `built`, with a "What shipped" section). Added a per-event `event_log.schema_version` stamp (column; per-type version in `EventTypes`; backup format bumped to v3, restores v2+v3). `LegacyEventMigration` + `PostgresPersister.migrateEventPayloads` rewrite each stale row's payload and stamp in one transaction (identity columns untouched); idempotent, validate-then-apply, read-only-guarded; OWNER-only `/admin/migrate-legacy-events` (GET preview + POST) with an `AuthorizationMatrixTest` row and admin nav card. Decisions settled with Ted: column not payload-key; in-place UPDATE; FQCN `type` normalization deferred. The versioning *framework* was deferred here but is **now built** (`2026-08-18`, shaped by the `format` v2→v3 migration) — `EventPayloadUpcaster` is a version-ladder composite of `EventUpcaster` rungs; see `EventPayloadUpcasterDesign.md`. Retirements (the `*TimeZoneUpcaster` rung classes, FQCN mapping, Antwerp resolver hacks) still gated on old backups leaving rotation. Detail in `Cleanup_Tasks.md`. |
| Boot-replay preflight (pre-deploy) | `done 2026-08-16` — `BootReplayPreflightTest`, a `@Tag("replay-preflight")` tier excluded from the default build (`mvn test -Preplay-preflight -Dpreflight.dump=…`; no dump ⇒ skips). Restores a prod backup into a scratch Testcontainer DB (validate pass = the exact upcast→classFor→bind boot uses) then drives `loadAllEvents()`, failing with the offending row named. Verified against clean + bad dumps (the 2026-08-16 Morocco/Antwerp failure mode). Certifies each retirement the eager migration unlocks; `/admin/zone-audit` is not a substitute. |
| `ConfirmedCalendar*` → `Calendar*` | `done 2026-08-19` — "confirmed" distinguished nothing: the route was already `/calendar` and the adjective survived only in class names. `ConfirmedCalendarRenderer` → `CalendarRenderer` (+ its one call site in `CalendarController`), and the three tests → `CalendarRendererTest`, `CalendarDayMenuJsTest`, `CalendarToggleJsTest`. Pure rename: no route, template, CSS class, event, or stored-data impact, and no collision (`CalendarViewBuilder` is a different thing). Docs naming the class swept. 962 unit + 36 js green, both js tests running under their new names. |
| `locationForMatching` may be droppable from hotels | `decision` — raised with the conference item, and the evidence is stronger than expected (checked 2026-08-20): **`ScheduleGapProjector` is its only reader**; `AddressParseService:89-95` fills it with the *same* `coalesce(locality)` it puts in `city`, so every geocoded address already has the two identical; and `Address`'s compact constructor falls back to `city` when blank. So it only ever does anything when **hand-edited** — and that hand-edit is the widen-a-suburb-to-its-metro move ground transfer now answers honestly, which hides a real journey. What would be lost is *normalizing* two spellings of one place ("Frankfurt" vs "Frankfurt am Main"), a different job that the paste widget mostly already does; only a hand-typed address can still disagree with itself. Two scopes: **stop offering the input** on the five forms that expose it (no schema change, fully reversible, the field just always equals the city) — recommended; or **remove it from `Address`**, which is an R6 event-schema change touching an upcaster, every golden sample and **backup-file compatibility**, for a field that costs nothing at rest — probably never. |
| Conferences have no `locationForMatching` | `decision` — **may no longer be needed (Ted, 2026-08-20)**. `PlanConferenceRequest:130` always passes `null`, so a conference matches on its city alone and **Lone Tree, CO** never matches the **Denver** a `DEN` flight resolves to; `/schedule-problems` then reported missing travel (found 2026-08-20). But ground transfer now answers that case honestly — there really *is* a hop from DEN to a Lone Tree venue, and recording it closes the gap; setting `locationForMatching` to "Denver" would have **hidden a real journey** instead, and a silenced gap is indistinguishable from one that was never there. The same objection applies to using the field on a hotel. What might still justify it is narrow — two nearby addresses under different city names with no journey between them at all — and it is not the case that raised the item. Decide before building; if dropped, delete it and record why here. |
| Action affordances that still move | `open` — the standing rule is in CLAUDE.md and `/conferences` was fixed 2026-08-19 (two virtual slots, greyed `Confirm` with its reason). Two known violations left alone: `PlannedGatheringsRenderer.actionsCell` (`:157`) moves `Edit` between lines depending on an optional `Event page →`, and `ItineraryRenderer`'s train card (`:163`) slides the OWNER pencil when a train has no service id. |
| "Restart needed" banner after a truncate | `open` — `/admin/database/truncate` empties the tables while `EventStore` and every projector keep the old data, so the app serves read models for events that no longer exist and only a restart clears it. Bites Ted's wipe-then-import workflow every time. Detection is derivable (persisted count lower than in-memory); render like the read-only banner, not as a post-deploy task. Split out of `PostDeployTaskBannerPlan.md` decision 4, which deliberately excludes it. |
| Shared admin nav bar | `open` — three hand-rolled shapes across the admin pages, and `admin-calendar-feed` / `admin-restore` / `admin-restore-success` have **no nav at all** (verified 2026-08-21). Extract one Thymeleaf fragment, as `Page.viewNav` already does for the eight j2html view pages. |
| Itinerary: add-entry day dropdown | `open` — `/calendar` has a future-day disclosure menu; `/itinerary` has no equivalent. Reuse the `DAY_MENU` pattern, OWNER-only. |
| Itinerary: where he is on an eventless day | `done 2026-08-21` — a stay's middle nights had no entries at all, so the itinerary went blank on exactly the days Ted is somewhere. A fully empty day now renders a tinted `.whereabouts` row in the empty-day slot, in one of three shapes: in a stay, two green lines with a hotel icon (`In Frankfurt, DE` over the hotel name); away with no bed, two **amber** lines plus an OWNER-only `Book hotel →` link (`In Denver` over `No hotel booked`); at home, one green line with a house icon (`You’re Home`). From `OngoingStay` + `ItineraryProjector.ongoingStayOn(date)` (derived from hotel entries already held), `ScheduleGapProjector.missingHotelOn(date)`, and `.atHomeOn(date)`. The no-bed row reads the `MissingHotel` read model and gets its links from `ProblemFix`, so it and `/schedule-problems` can never offer different dates or a different destination for the same gap. **The home claim needs positive evidence:** absence of an away band is not enough — a trip with no return booked bands nothing — so a fourth cached read model asks whether each day's *last* fact left him in a home city, and both must hold. Check-in/check-out days excluded (their own entries say it better); days with *any* entry untouched, per Ted. Detail in `Cleanup_Tasks.md`. |
| Read-only redirect untested on the conference controllers | `open` — `ConfirmConferenceAttendanceController` and `DeclineConferenceController` both catch `ReadOnlyModeException` and redirect, and neither slice test exercises the branch (still true 2026-08-21); the `catch` could be deleted with both suites green. Worth checking the other write controllers for the same hole. |
| Malformed conference id on the GET of `/conferences/{id}/confirm` | `open` — only the POST path has a `malformedConferenceIdRedirects…` test, so the GET-side catch is unpinned. Same on `/decline`. |
| Mockito replacement | `open` — replace with better test doubles. |
| Event-type filter on `/admin/eventlog` | `open` — the command-log filter is already done. |
| `/admin/commandlog` "Out of order" badge | `open` — only detects divergence *within* a page; `PostgresPersister.loadTimelinePage` resets `runningMaxSeq` per call (`PostgresPersister.java:291`), so the first entry of any page can never be flagged. |

### Deferred until needed — not queued

`Cleanup_Tasks.md` ends in a **Deferred (until needed)** section: items with a known shape and a
named trigger, deliberately not worked down. They are listed here only so this index matches that
file; do not promote one without its trigger firing.

| Item | Trigger |
|---|---|
| **Private events in `DifferentCityConflict`** — detection is already indifferent to entry kind, but the *clearing* event types its subject as a `GatheringId`, so a private event in the wrong city would be unclearable. Additive fix: a `PrivateEventCityConflictCleared` (or a one-of subject) plus the detector branch; nothing stored changes. Tabled by Ted 2026-08-20 after problem-calendar slice 4 shipped without it. | Ted plans a private event on a day a conference runs elsewhere and wants the clash surfaced. |
| **Change a ground transfer** — the other half of D11 in `archived/GroundTransferPlan.md`. Cancel shipped 2026-08-20 and took the urgency with it: correcting a transfer is cancel-then-enter, and both ends are snapshots by design, so nothing is lost in the round trip. | Ted re-enters the same transfer often enough to notice — most likely once a mode/notes field (D7) arrives. |
| **No logout affordance, and `GET /logout` is a 404** — deferred by Ted 2026-08-21, incognito is sufficient. `POST /logout` works; the custom login page (`0435623`) dropped Spring's generated logout page along with its generated login page, leaving GET unmapped. Rides along: `login.html`'s `?logout` notice is unreachable because `logoutSuccessUrl` points at `/`, while a test pins the notice — the two disagree. | A second person needs an account, or Ted wants to switch roles where a private window is awkward (the iPad). |

### Loose follow-ups not tracked anywhere else

From the Phase 1 `cancelBy` review (bottom of `HotelCancelReplacePlan.md`):

- Editing check-in earlier than an existing `cancelBy` fails on a field the user never touched
  (the form prefills it) — `ChangeHotelCommand.java:43`. Accepted behavior, not a bug; the
  alternative is clamping rather than rejecting.

*(The other two items on that list — the wrong cancel-by hint text and the duplicated
`cancelBy(LocalDateTime, ZoneId)` helper — were fixed inside `4efccaf` before it was committed.
The review had been written against the pre-fix working tree.)*

From the Cancel Hotel slice (2026-08-07):

- **`ChangeHotel` and `ChangeFlight` still decide from a projector**, which R1 in
  `EventSourcingRulesHeuristics.md` forbids ("never use a projection to make an automated
  decision"). Both read a details projector for an existence check; `CancelHotel` now folds from
  the event stream via `CommandExecutor.eventsForDecision()` and is the pattern to follow. Ted
  asked for this follow-up when choosing the fold for Cancel. Low risk today (the existence check
  is not a time gate and subscribers are synchronous), but the codebase currently contradicts its
  own rule doc. **Now owned by `DecisionContextQueryDesign.md`**, which replaces both the projector
  reads and `eventsForDecision()` with a tagged query — **unblocked 2026-08-10** (see next).
- **Export/import → event-oriented backup/restore. `done 2026-08-11` — owning doc `archived/EventOrientedBackupRestorePlan.md`.**
  Ted's call (2026-08-10): events are the source of truth, so backup/restore stores and restores
  events **verbatim** and stops re-executing commands; commands stay in the backup as opaque history
  for a future undo feature. This resolves the "wider decision before more commands need folded
  context" question that had blocked the decision-context query design — with no command replay on
  restore, there is no import context to fake. `DecisionContextQueryDesign.md` is therefore
  unblocked.

From `archived/GeneralControllerRefactorPlan.md`:

- Stable `data-testid` attributes on the `index.html` nav groups, so the authorization tests stop
  asserting on `href` substrings and `>Admin</span>`.

From `archived/j2html_Migration_Analysis.md`:

- The shared renderer infrastructure the migration proposed was never extracted — no
  `TemporalFormatter`, `ProblemCardRenderer`, or `EntryCardRenderer`; only `web/Page.java` exists.
  Formatting and card markup are duplicated across renderers.

## Decisions waiting on Ted

| Decision | Owning doc |
|---|---|
| **Adopt Strictland, or revert the spike?** The mapper pin (`EventJsonMapperFactory`) stays either way. Adoption is paused pending the author's feedback. If adopted: pick the first events to cover, the snapshot-file layout, and how coverage is enforced for new event types. | `Event_Serialization_Contract_Tests.md` |
| **Alpha-2 zone aliases for single-zone countries** (e.g. `GB`) — the last piece of the `LocationZoneResolver` state/province fix that unblocked the 2026-08-06 production import. | `UtcDatetimeStoragePlan.md` (improvement 2) |

## Explorations — deliberately unbuilt

These are design records. Do not implement without discussing with Ted first; several say so
explicitly in their own text.

| Doc | What it is |
|---|---|
| `CommandConsistencyEventStore.md` | Conditional/fenced append for a future multi-instance deployment. Nothing built. Unblocked by the CommandExecutor migration, but not committed to. |
| `DataStarResearchPlan.md` | `exploration 2026-08-20` — replace the 8 inline-JS UI behaviors (dropdown prefill, toggles, address fetch-and-fill) with Datastar? Recommendation: **not now** — free tier fits only 3 of 8 (the largest, the browser-zone script, needs paid Pro), tests move rather than disappear, and it adds 1–2 dependencies. Revisit triggers: a slice needs SSE server-push, cascading form interactivity, or the inline-script count doubles. Pilot order if adopted: address paste-parse → ground-transfer prefill (backend-driven) → calendar toggle. |
| `DecisionContextQueryDesign.md` | `unblocked — ready to build` (was `paused`) — replace `CommandExecutor.eventsForDecision()` and the projector-based existence checks with a tagged/typed query port. Design close to settled (recommendations + open decisions recorded); the export/import blocker is resolved by `archived/EventOrientedBackupRestorePlan.md` (2026-08-10). Owns `DecisionsToReview.md` D1 and D2. |
| `TaggedEventStoreQueryingDesign.md` (**repo root**, not `docs/`) | Filtered `EventStore` queries by event type and tag, pushed into Postgres JSONB + GIN. Deferred until a second concrete caller demands it. Its `@EventName` proposal is now **obsolete** — `EventTypes` does that job. Its "every id is a tag" rule and multi-valued tag shape are live input to `DecisionContextQueryDesign.md`. |
| `ReMoDeL-Specification.md` | A KDL-based read model definition language. Specification only — there is no KDL code anywhere in `src/`. |
| `ReadModelKdlTestDslPlan.md` | Test-support DSL for the above. Its 8 implementation steps reference an acceptance test that does not exist yet. Blocked on ReMoDeL itself. |
| `RestoreCompatibilityFloorPlan.md` | `design 2026-08-19, nothing built` — can restore refuse a backup that predates a **retired** upcaster rung? **It already does:** pass one's climb fails loud, so the file is rejected with zero writes and every problem reported, and `/admin/restore/validate` is the dry run. What is missing is *diagnosis* — the refusal reads as ~200 lines of per-event "payload cannot be restored" instead of "this file is too old". Proposes a per-type `minimumRestorableSchemaVersion` in `EventTypes` (bumped in the same commit that deletes a rung) plus one aggregated verdict naming the count, an example row and the remedy. Format-version bump, a global minimum, a date-based floor and auto-upgrade were all rejected, with reasons. **Build trigger: the first rung retirement, same commit** — every floor is 1 until then. Blocked on the undrawn retirement date (`MigrationLessonsLearned.md` follow-up 3). One test in it is worth doing now regardless: assemble a ladder with a rung omitted and pin the fail-loud message. |
| `Refactoring_Opportunities.md` | 7 projection/rendering duplication findings with a priority order. Marked "do not implement without discussion". |
| `RendererVsProjectorResponsibilities.md` | `discussion` — what is a renderer's job vs. a projector's, and which should a controller depend on? Opened 2026-08-18 out of the reverted state-aware nav link (seven controllers gained a `ScheduleGapProjector` for one nav fact). Four options sketched (status quo / renderer-holds-projector / renderer-as-instance / a page-model layer); nothing decided. Ted wants to pick this up later. Concrete trigger to revisit: the first time a renderer needs a real collaborator. **Reopened 2026-08-19** on a second, different occasion — `CalendarEntry` growing a `commitment` field that applies to one `EntryKind` only, at the start of slice 2 of `ConferenceSubmissionTrackingPlan.md`. That section separates two problems (record shape vs. redaction-after-the-fact), sketches E1–E4 (shape), M1–M2 (how much a projector may render), S1–S2 (per-audience projection, which would delete `CalendarEntryRedactor`), a precise change list for S2, and answers a side question on `EventStreamConsumer` advertising event interest (recommendation: don't). **DECIDED 2026-08-19: S2 + E2** — one `PublicCalendarProjector` (deletes `CalendarEntryRedactor`), core `CalendarEntry` + sealed `EntryDetails`, `kind()` derived from `details` so the two hierarchies can't drift; E1 lost because the public side must collapse many private kinds to one "Busy" shape and its security invariant would grow with every kind. **Commit split decided 2026-08-21: E2 first**, so the big mechanical diff lands while the redactor's compile-time forcing function is still armed, and the security change is then a small focused diff. **Commit 1 (E2) SHIPPED 2026-08-21**: `CalendarEntry` went from **13 fields + 4 convenience constructors** (worse than this doc recorded — ground transfer had added `publicRoute` and `cancelPath`) to **7 fields + 1**, with a sealed `EntryDetails` holding everything kind-specific and `kind()` derived from it. The redactor now switches on the details type (exhaustive over the sealed type, so the forcing function is per-kind and sharper); `CalendarViewBuilder` gained three exhaustive switches (`titleLink`/`ownerActions`/`badges`) in place of four flat field reads; the gathering's link is now honestly named `infoUrl`. Two redaction tests were **deleted as unwriteable** — including the five-kind commitment loop, replaced by `redactionNeverChangesTheKind`, which does not grow with the kinds. Both tiers green (1343 + 50), six mutations verified. **Commit 2 (S2) is next**: one `PublicCalendarProjector` + four public details types behind a `PublicDetails` marker, controller picks source by audience, redactor deleted — plus a one-off equivalence test (public projector vs. redactor, per kind) deleted in that same commit. The *original* renderer-vs-projector question (options A–D) remains undecided. |
| `authorization_policy_centralization.md` | Authorization "Option 2" — derive `SecurityConfig` matchers and `GeneralController` nav flags from one `NavArea` policy type. Current Option 1 (two small hand-synced places) is judged low-risk because `AuthorizationMatrixTest` catches drift. This doc is the live tracker; the duplicate checklist inside `archived/GeneralControllerRefactorPlan.md` is not maintained. |

## Done — moved to `archived/`

Kept as design records. Each has a status banner at the top of the file.

**As of 2026-08-21 these live in `docs/archived/`** — see `archived/README.md`. Nothing was
deleted, and the rows below still point at the owning doc; only the path changed. A doc goes there
when it owns no remaining work: the six that shipped with a follow-up of their own had that
follow-up **lifted into `Cleanup_Tasks.md` first**, so archiving never drops an open item.

Two rows below have **no** `archived/` prefix and are deliberately still in `docs/`:
`UtcDatetimeStoragePlan.md` still owns an open decision (alpha-2 zone aliases), and
`EventPayloadUpcasterDesign.md` is a live how-to — how to add and retire an upcaster rung — not a
finished plan.

| Doc | Landed |
|---|---|
| `UtcDatetimeStoragePlan.md` | All 7 phases + review fixes. `25104b9`, 2026-08-05. **Stays in `docs/`** — still owns the alpha-2 zone-alias decision. |
| `archived/UtcDatetimePlanReview.md` | Folded into the plan above; historical record, checkboxes no longer maintained |
| `archived/GatheringConferenceUtcRolloutPlan.md` | Gathering `ddf4ba8` (2026-07-27), conference `67a47af` (2026-08-05) |
| `archived/TrainFlightUtcRolloutPlan.md` | Trains `daa7107`, flights `d2884fb`, 2026-06-24 |
| `archived/HomeCityPlan.md` | `26d15ac`, 2026-07-26 |
| `archived/ChangeGatheringPlan.md` | `f28cb49`, 2026-07-25 |
| `archived/ChangeHotelPlan.md` | `d98af11`, 2026-06-21 |
| `archived/ChangeTrainPlan.md` | `8390262`, 2026-06-17 |
| `archived/Remove-Local-Profile.md` | `e962915`, 2026-06-17 |
| `archived/j2html_Migration_Analysis.md` | `b0e6f11`, 2026-06-06 — views migrated, shared infrastructure not extracted (see loose follow-ups) |
| `archived/GeneralControllerRefactorPlan.md` | Shipped; its "open items" 1 and 2 are resolved, item 3 moved here |
| `archived/EventOrientedBackupRestorePlan.md` | `done 2026-08-11` — event-verbatim backup/restore replaced command replay. Full row under **Open features** above. |
| `archived/LegacyEventEagerMigrationPlan.md` | `built 2026-08-16` — eager legacy-row migration + per-event `schema_version` stamp + boot-replay preflight. |
| `archived/CalendarSubscriptionFeedPlan.md` | `done 2026-08-18` — Phase 1 (cancel-deadline reminders), validated on-device in prod. **Phase 2 (full travel calendar) lifted to `Cleanup_Tasks.md`.** |
| `archived/PrivateSocialEventPlan.md` | `done 2026-08-13` — `PRIVATE_EVENT` kind, its own redacting branch, itinerary entry. **Change + list view lifted to `Cleanup_Tasks.md`.** |
| `archived/ScheduleProblemsRewritePlan.md` | `shipped 2026-08-20` — all seven slices; detection rebuilt on one located timeline. **The mirror of D14 lifted to `Cleanup_Tasks.md`.** |
| `archived/ProblemCalendarPlan.md` | `done 2026-08-20` — slices 1–5, ending with clash markers and fix links. **One of its two open questions is answered** (the calendar is the default view, 2026-08-21); the day-number link stays in `Cleanup_Tasks.md`. Two of its decisions are annotated as reversed there: F9's one-control-per-card menu, and F1's per-kind band fills. |
| `archived/GroundTransferPlan.md` | `done 2026-08-20` — plan + cancel. Change was already tracked in `Cleanup_Tasks.md` (Deferred). |
| `archived/CalendarAwayBandPlan.md` | `done 2026-08-21` (`d4a9a3f`) — turquoise away stripe on `/calendar`. No tail: the schedule-problems variant was **closed** by Ted 2026-08-21, not deferred. |
| `archived/EventTypeColumnNormalizationPlan.md` | built 2026-08-19, **run in production 2026-08-21**. Full row under **Open features** above. |
| `EventPayloadUpcasterDesign.md` | Reference doc for the read-time version-ladder. The upcaster framework `archived/LegacyEventEagerMigrationPlan.md` deferred, now built (2026-08-18): `EventPayloadUpcaster` is a composite of per-`(type × step)` `EventUpcaster` rungs. How-to-add / how-to-retire a rung lives here. |

## Shipped without an owning plan doc

Small-to-medium slices that went straight from conversation to code, so no plan doc names them.
Recorded here only so this index matches the tree — none of them has open work unless a row says so.

| What shipped | Landed |
|---|---|
| **Viewer-zoned "today" + custom login page.** The calendar/itinerary computed *today* in the server's JVM zone (UTC on Railway), flipping a day early each Pacific evening. Now `ViewerTodayZone` resolves a `ZoneId` from a `viewerZone` cookie (else `jittertravel.today.fallback-zone`, default `America/Los_Angeles`); controllers compute `LocalDate.ofInstant(clock.instant(), zone)`. The server can't read the browser zone unaided, so a **custom login page** (`LoginController` + `templates/login.html`, replacing Spring's generated one) carries a hidden `browserZone` field JS fills, and `ZoneCapturingAuthenticationSuccessHandler` writes the cookie on the login-success response — correct on first paint even for a deep link bounced through login. Cookie is set **only** on an authenticated response, so anonymous visitors never receive one. Also: calendar default `from` is now one week before today. Documented as **decision 10** of `UtcDatetimeStoragePlan.md` (a later increment to that otherwise-`done` plan). Advances the **Viewer-timezone selection** open row above (only the zone-override dropdown remains). | `0435623`, 2026-08-16 |
| **Decline Conference slice.** Ted's own "decided not to attend" (accepted to Devoxx Morocco, then withdrew), kept DISTINCT from organizer `ConferenceCancelled`. `ConferenceAttendanceDeclined` event + `DeclineConference` command/service/context (folds "still live?" from the event stream) + `DeclineConferenceController` and a Decline action on `/tentative-conferences`; four projectors drop the conference. Uses `ConferenceSubmissionTrackingPlan.md`'s own event name (forward-compatible with that plan's full two-dimension status, still open). New `/tentative-conferences/*/decline` route with its own `SecurityConfig` matcher + `AuthorizationMatrixTest` row. | `c08896a`, 2026-08-16 |
| **Multi-segment flight lookup offers each leg.** AeroDataBox returns one entry per segment for a flight number; the client used to collapse them into leg 1's departure and leg N's arrival, inventing a route no leg flies (UA 1604 → `RDU → RNO`). `FlightLookupCandidates` now carries every segment and reports whether a choice is needed; `book-flight` / `change-flight` render a shared "Which flight?" fragment and post the chosen segment back to `.../lookup/select` (no second API call). The whole trip is offered as an extra option only when the segments chain end to end. Also seeds the lookup date from `?date=` or the booking's own departure-zone day. **New route `/booked-flights/*/lookup/select`** — two segments past the id, so no existing matcher covered it; it has its own `SecurityConfig` line and `AuthorizationMatrixTest` row. | `a2b1845`, 2026-08-14 |
| **Owner home page redesign** — two-column viewing/editing layout for OWNER. The nav-card items in `Cleanup_Tasks.md` (state-aware "Schedule problems" card) still apply to it. | `5277366`, 2026-08-13 |
| **Owner future-day create menu on the calendar** — clicking a future day offers "Add flight / gathering / conference…", seeding each form via `?date=`. | `93a78ad`, 2026-08-12 |

## Not a task list

These describe how things are, not what to do next, and belong in no status column:
`JS-Behavior-Tests.md`, `EventSourcingRulesHeuristics.md`, `MigrationLessonsLearned.md`,
`DEPLOYMENT.md`, `lodging-event-model.md`,
`ChangeFlightSlices.md`, `Features.md`, `EventModel.txt`, `EventLanesDoc.txt`,
`Tentatively-Plan-Conference-slice.txt`, `docs/slices/`.
